id = "coherence-guard-reverse-direction"
name = "issue #973: T16.4 coherence guard also asserts every real apply flag is documented somewhere (not just fabrication-detection)"

[[predicate]]
id = "reverse-coherence-check"
provider = "custom_script"
description = "TASK BRIEF (github issue #973): test/kazi/teach_coherence_test.exs (T16.4) only catches FABRICATED doc references (a command/flag named in SKILL.md/AGENTS.md that doesn't exist in the real CLI) -- it never asserts the reverse: that every real, shipped flag is documented SOMEWHERE. This let `--allow-primary-workspace`/`--allow-duplicate-run` ship with zero doc coverage. Already researched -- exact seams, do not re-derive:\n- test/kazi/teach_coherence_test.exs:42-61 `cli_surface/0` (calls `Kazi.CLI.run([\"help\",\"--json\"])`, builds `%{commands: MapSet, flags: MapSet}`); :79-83 `code_contexts/1` (extracts fenced/inline code spans -- fabrication-detection only scans CODE contexts); :95-104/:113-122 `referenced_commands/1`/`referenced_flags/1` (regex-extract from those code contexts); :131-151 `assert_coherent/3` (the one-directional doc->CLI assertion); describe blocks at :157/:219/:250.\n- Real flag source of truth: lib/kazi/cli.ex:111-154 `@switches` (every flag name/type); :161-238 `@flag_docs` (one description per flag; `allow_primary_workspace` at 222-223, `allow_duplicate_run` at 224-225); :249-277 `@commands` (per-command `flags:` list -- the `apply` entry's `:allow_primary_workspace`/`:allow_duplicate_run` at 274-275); :1317 `help_json`/:1331 `command_json/1` assemble these into the `kazi help --json` payload `cli_surface/0` already consumes.\n- Rendered SKILL.md accessor: lib/kazi/teach/install_skill.ex:93 `skill_md/0` (no file I/O, direct call, used at teach_coherence_test.exs:159,175); AGENTS.md is read via `File.read!(\"AGENTS.md\")` (teach_coherence_test.exs:166,189).\n- Allow-list template to reuse for the shape (not the domain): lib/kazi/reconcile/coverage.ex:50-60/108-120 (ADR-0021's `Coverage.check(surface, predicates, allow_list: [...])`, string patterns with optional trailing `*` wildcard, partitions into owned/allowed/unowned via a `Result` struct at 82-106).\n- Prior ad hoc precedent (to fold into the new generic guard, not duplicate): test/kazi/teach/default_tier_test.exs:148-155 hand-asserts presence of the two specific flags in `@skill_md`/`@agents_md` -- the new guard should make this kind of per-flag hand-check unnecessary going forward.\nADD a new describe block (same file or a sibling test/kazi/teach_coherence_reverse_test.exs) that: iterates every flag in the `apply` command's real flag set (from `cli_surface/0`'s command_json for `apply`, or directly from `Kazi.CLI`'s command table -- reuse `cli_surface/0`, don't re-implement JSON parsing), and asserts each flag's literal string (e.g. `--allow-primary-workspace`) appears SOMEWHERE in the combined SKILL.md + AGENTS.md text, UNLESS it's on a new, small, explicit, reviewed `@undocumented_flags_allowlist` module attribute (for genuinely internal/debug-only flags -- keep this allow-list short and comment WHY each entry is there). A new flag added to `apply` with no doc mention and no allow-list entry must FAIL this test. Verify it actually catches the historical gap: temporarily confirm (in your own scratch testing, not committed) that removing `--allow-primary-workspace` from the skill text would fail the new test, then leave the skill text as-is (already documented on main) so the real test run is green. PROCESS: branch task/coherence-guard-reverse-direction, small conventional commits, push with -u. This predicate passes when the new test passes."
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix test test/kazi/teach_coherence_test.exs test/kazi/teach_coherence_reverse_test.exs 2>/dev/null || env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix test test/kazi/teach_coherence_test.exs']
timeout_ms = 300000

[[predicate]]
id = "full-suite-green"
provider = "custom_script"
description = "GUARD: the full ExUnit suite stays green."
guard = true
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix test']
timeout_ms = 600000

[[predicate]]
id = "format-clean"
provider = "custom_script"
description = "GUARD: mix format --check-formatted stays clean (CI enforces it)."
guard = true
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix format --check-formatted']
timeout_ms = 120000

[[predicate]]
id = "landed"
provider = "custom_script"
description = "The work is COMMITTED and PUSHED: working tree clean, an upstream is set, and HEAD equals the upstream ref."
cmd = "sh"
args = ["-c", 'test -z "$(git status --porcelain)" && git rev-parse --abbrev-ref @{u} >/dev/null 2>&1 && [ "$(git rev-parse HEAD)" = "$(git rev-parse @{u})" ]']
timeout_ms = 30000
