id = "sonnet-default-tier-flip"
name = "issues #955-#958: the rendered skill + AGENTS.md teach sonnet-first tiering, the safety-refusal flags, current subsumption status, and live-economy grounding"

[[predicate]]
id = "default-tier-test"
provider = "custom_script"
description = "TASK BRIEF (github issues #955, #956, #957, #958 -- one coordinated change to the self-teaching surfaces: lib/kazi/teach/install_skill.ex, AGENTS.md, and the ADR-0035 record; read all four issues first, they contain the evidence): (1) #956 -- the install-skill generator still teaches haiku-first tiering; flip it: claude-sonnet-5 is the DEFAULT grind tier, escalation ladder sonnet -> claude-opus-4-8, haiku an explicit opt-down for trivial slices only; every example invocation and the ladder shell snippet updated. (2) #958 -- do NOT bake a frozen date-stamped fleet snapshot into the skill prose; the tiering rationale must point at live re-derivation via `kazi economy --json` (per-model/goal-shape cost + wall-clock percentiles). The dated 2026-07-08 finding (haiku ~37% stuck+over_budget vs ~13% sonnet) belongs ONLY in a dated amendment note appended to docs/adr/0035-* (append, never rewrite the ADR body). Cite #924 for the vacuous-convergence rationale, not #936 (per the #956 citation note). (3) #955 -- the generated skill must teach the two apply safety refusals: --allow-primary-workspace (primary-worktree refusal, prefer `git worktree add`) and --allow-duplicate-run (LIVE-run duplicate refusal, zombie rows age out ~90s), including what each refusal means and that reflexively adding the flag defeats the protection. (4) #957 -- replace the stale '(coming)' ADR-0031 subsumption framing: the E21/E23 proofs (T21.12, T23.9) landed 2026-06-26 on released v1.64.2, so state that `kazi apply --parallel` subsumption is PROVEN for wave execution -- but pair it with the honest open caveat from #936 (no supervised checkpoint between needs-DAG waves yet; a bad upstream wave can cascade into dependent waves). Update the same claim in AGENTS.md and, if docs/plan.md risk row R-E26-1 still carries the '(coming)' gate, update that row too. Write test/kazi/teach/default_tier_test.exs asserting on the RENDERED install-skill output and AGENTS.md: (a) claude-sonnet-5 is the default grind tier and no haiku-first ladder remains; (b) both safety-refusal flags are taught; (c) no '(coming)' subsumption gate text remains and the #936 checkpoint caveat is present; (d) the tiering rationale references `kazi economy`; (e) every model id named is a real current id. PROCESS: branch task/sonnet-default-tier-flip, small conventional commits (lib/ vs test/ vs docs/ vs AGENTS.md separate), push with -u. This predicate passes when the new test file passes."
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix test test/kazi/teach/default_tier_test.exs']
timeout_ms = 300000

[[predicate]]
id = "teach-coherence-green"
provider = "custom_script"
description = "GUARD: the skill/AGENTS.md <-> CLI coherence suite stays green (T16.4/T30.5 gates)."
guard = true
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix test test/kazi/teach_coherence_test.exs']
timeout_ms = 300000

[[predicate]]
id = "full-suite-green"
provider = "custom_script"
description = "GUARD: the full ExUnit suite stays green."
guard = true
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix test']
timeout_ms = 600000

[[predicate]]
id = "format-clean"
provider = "custom_script"
description = "GUARD: mix format --check-formatted stays clean (CI enforces it)."
guard = true
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix format --check-formatted']
timeout_ms = 120000

[[predicate]]
id = "landed"
provider = "custom_script"
description = "The work is COMMITTED and PUSHED: working tree clean, an upstream is set, and HEAD equals the upstream ref."
cmd = "sh"
args = ["-c", 'test -z "$(git status --porcelain)" && git rev-parse --abbrev-ref @{u} >/dev/null 2>&1 && [ "$(git rev-parse HEAD)" = "$(git rev-parse @{u})" ]']
timeout_ms = 30000
