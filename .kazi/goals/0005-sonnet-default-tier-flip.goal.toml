id = "sonnet-default-tier-flip"
name = "repo-side tiering flip: the rendered skill + AGENTS.md teach claude-sonnet-5 as the default grind tier (haiku = explicit opt-down)"

[[predicate]]
id = "default-tier-test"
provider = "custom_script"
description = "TASK BRIEF (operator-gated; fleet data 2026-07-08: haiku ~37% stuck+over_budget vs ~13% sonnet across ~100 finished runs, making haiku a net cost LOSS on non-trivial slices): flip the repo-side tiering guidance to match the already-updated global skill. Changes: (1) lib/kazi/teach/install_skill.ex -- the rendered SKILL.md teaches claude-sonnet-5 as the DEFAULT grind tier, escalation ladder sonnet -> opus-4-8, haiku demoted to an explicit opt-down for trivial slices only; all examples use sonnet; (2) AGENTS.md -- same flip, same rationale; (3) docs/adr/0035-* gets a short amendment note recording the fleet-data-driven flip (do NOT rewrite the ADR -- append a dated note); (4) the T30.5 tiering accuracy/coherence CI gate must stay green (test/kazi/teach_coherence_test.exs and related suites). Write test/kazi/teach/default_tier_test.exs asserting: (a) the rendered skill content names claude-sonnet-5 as the default grind tier and does NOT teach haiku-first; (b) AGENTS.md agrees (coherence across both surfaces); (c) every model id referenced is a real current id. PROCESS: branch task/sonnet-default-tier-flip, small conventional commits (lib/ vs test/ vs docs/ vs AGENTS.md separate), push with -u. This predicate passes when the new test file passes."
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix test test/kazi/teach/default_tier_test.exs']
timeout_ms = 300000

[[predicate]]
id = "teach-coherence-green"
provider = "custom_script"
description = "GUARD: the skill/AGENTS.md <-> CLI coherence suite stays green (T16.4/T30.5 gates)."
guard = true
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix test test/kazi/teach_coherence_test.exs']
timeout_ms = 300000

[[predicate]]
id = "full-suite-green"
provider = "custom_script"
description = "GUARD: the full ExUnit suite stays green."
guard = true
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix test']
timeout_ms = 600000

[[predicate]]
id = "format-clean"
provider = "custom_script"
description = "GUARD: mix format --check-formatted stays clean (CI enforces it)."
guard = true
cmd = "sh"
args = ["-c", 'env -i HOME="$HOME" PATH="$PATH" LANG="$LANG" MIX_ENV=test mix format --check-formatted']
timeout_ms = 120000

[[predicate]]
id = "landed"
provider = "custom_script"
description = "The work is COMMITTED and PUSHED: working tree clean, an upstream is set, and HEAD equals the upstream ref."
cmd = "sh"
args = ["-c", 'test -z "$(git status --porcelain)" && git rev-parse --abbrev-ref @{u} >/dev/null 2>&1 && [ "$(git rev-parse HEAD)" = "$(git rev-parse @{u})" ]']
timeout_ms = 30000
