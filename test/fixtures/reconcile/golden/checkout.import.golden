[
  {"group",
   [
     [{"id", "checkout"}, {"name", "Checkout"}],
     [{"id", "sign-up"}, {"name", "Sign Up"}],
     [{"id", "ungrouped"}, {"name", "Ungrouped"}]
   ]},
  {"id", "gherkin-import"},
  {"mode", "create"},
  {"name", "Checkout"},
  {"predicate",
   [
     [
       {"acceptance", true},
       {"args",
        ["-c",
         "echo 'kazi: behavior-spec scaffold — replace cmd/args with the real check for this scenario (its Given/When/Then steps are recorded on this predicate).' >&2; exit 1"]},
       {"cmd", "sh"},
       {"description",
        "A shopper checks out a basket: Given a basket with two items; When the shopper pays with a valid card; Then the order is confirmed; And a receipt is emailed"},
       {"feature", "Checkout"},
       {"group", "checkout"},
       {"id", "checkout__a-shopper-checks-out-a-basket"},
       {"provider", "custom_script"},
       {"scenario", "A shopper checks out a basket"},
       {"steps",
        ["Given a basket with two items",
         "When the shopper pays with a valid card",
         "Then the order is confirmed", "And a receipt is emailed"]},
       {"verdict", "exit_zero"}
     ],
     [
       {"acceptance", true},
       {"args",
        ["-c",
         "echo 'kazi: behavior-spec scaffold — replace cmd/args with the real check for this scenario (its Given/When/Then steps are recorded on this predicate).' >&2; exit 1"]},
       {"cmd", "sh"},
       {"description",
        "Payment is declined for a bad card: Given a basket with one item; When the shopper pays with a <card>; Then the payment is declined"},
       {"feature", "Checkout"},
       {"group", "checkout"},
       {"id", "checkout__payment-is-declined-for-a-bad-card"},
       {"provider", "custom_script"},
       {"scenario", "Payment is declined for a bad card"},
       {"steps",
        ["Given a basket with one item", "When the shopper pays with a <card>",
         "Then the payment is declined"]},
       {"verdict", "exit_zero"}
     ],
     [
       {"acceptance", true},
       {"args",
        ["-c",
         "echo 'kazi: behavior-spec scaffold — replace cmd/args with the real check for this scenario (its Given/When/Then steps are recorded on this predicate).' >&2; exit 1"]},
       {"cmd", "sh"},
       {"description",
        "A new user signs up: Given a visitor on the home page; When they submit the sign-up form; Then their account is created"},
       {"feature", "Sign Up"},
       {"group", "sign-up"},
       {"id", "sign-up__a-new-user-signs-up"},
       {"provider", "custom_script"},
       {"scenario", "A new user signs up"},
       {"steps",
        ["Given a visitor on the home page",
         "When they submit the sign-up form", "Then their account is created"]},
       {"verdict", "exit_zero"}
     ],
     [
       {"acceptance", true},
       {"args",
        ["-c",
         "echo 'kazi: behavior-spec scaffold — replace cmd/args with the real check for this scenario (its Given/When/Then steps are recorded on this predicate).' >&2; exit 1"]},
       {"cmd", "sh"},
       {"description",
        "An existing email is rejected: Given an account already exists for the email; When a visitor submits the sign-up form with that email; Then the sign-up is rejected"},
       {"feature", "sign-up"},
       {"group", "sign-up"},
       {"id", "sign-up__an-existing-email-is-rejected"},
       {"provider", "custom_script"},
       {"scenario", "An existing email is rejected"},
       {"steps",
        ["Given an account already exists for the email",
         "When a visitor submits the sign-up form with that email",
         "Then the sign-up is rejected"]},
       {"verdict", "exit_zero"}
     ],
     [
       {"acceptance", true},
       {"args",
        ["-c",
         "echo 'kazi: behavior-spec scaffold — replace cmd/args with the real check for this scenario (its Given/When/Then steps are recorded on this predicate).' >&2; exit 1"]},
       {"cmd", "sh"},
       {"description",
        "The health endpoint responds: Then the service reports healthy"},
       {"feature", ""},
       {"group", "ungrouped"},
       {"id", "ungrouped__the-health-endpoint-responds"},
       {"provider", "custom_script"},
       {"scenario", "The health endpoint responds"},
       {"steps", ["Then the service reports healthy"]},
       {"verdict", "exit_zero"}
     ]
   ]}
]