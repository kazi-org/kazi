
The research-grounded content rewrite. The shipped paradigm is "you CHAT with Claude
Code and the AGENT drives kazi" (the skill / `kazi mcp` / `--json` CLI -- E16 shipped),
but the README/site still LEAD with the legacy human -> kazi route. Deep research of
the fastest-growing OSS AI tools (docs/devlog.md 2026-06-24; ADR-0030) gives the
playbook: kazi's analogs are agent-FACING tools the user does not operate (Serena,
Context7) + Astral's proof discipline. This epic applies ADR-0030 to every surface.

The organizing spine of EVERY surface is **human -> Claude -> kazi -> Claude**: you
direct Claude Code in chat; Claude drives kazi ("have kazi drive this until done");
kazi reconciles until the predicates are objectively true; Claude reports back. The
raw `kazi` CLI is reframed as what the AGENT invokes (and a power-user/advanced
fallback) -- NOT the primary human interface. The superseded framing -- "a human
runs the kazi CLI" -- is demoted to a Reference/Advanced note everywhere.

It is the SINGLE CANONICAL docs+content epic (consolidated 2026-06-25): to stop
drafting successive generations of the same document, it ABSORBS the entire E22
pre-publish refresh (T22.1-T22.9, now retired) and the docs/positioning parts of
E17 (T17.1/2/4/5), E20 (T20.10 pool guide), E21 (T21.11 native-parallelism
positioning), E30 (T30.5 tiering accuracy gate), and E31 (T31.8 self-maintaining-docs
positioning). Each surface is written ONCE here -- one README rewrite, one website
rewrite, one docs/guide+concept alignment, one accuracy/publish gate -- not a rewrite
then a coverage pass then a positioning pass. ADR-0025's lead-order (agent first,
vanilla as reference) holds. No invented features -- agent-driving (skill/mcp/`--json`)
is REAL now; promised work labelled "coming" (ADR-0025). Mixed content + engineering.

- [x] T25.1 Tagline -- DECIDED (operator 2026-06-24): the line-1 hook is **Your coding agent says "done." kazi proves it.** (the precise category -- "the outer/reconciliation loop for coding agents" -- is the second beat). No longer a choice; POOL wires it verbatim into `site/src/canonical.mjs` (hero tagline) + the README H1 in lockstep.  Done 2026-06-25 (PR #454; coherence gate extended to enforce HERO_TAGLINE; verified live at https://kazi.sire.run -- hero H1 byte-identical).  Owner: TBD  Est: 0.5h  verifies: [UC-039]  delivers: [the decided tagline wired into the canonical strings]  deps: []  acc: README H1 + the site hero render `Your coding agent says "done." kazi proves it.` byte-identically; canonical strings match (coherence T9.9 green); the precise category appears as the second line.
- [x] T25.2 Hero asset -- the loop transcript -- DECIDED (operator 2026-06-24): RECORD A REAL CAST (no static fallback). Drive a real `claude -> kazi -> harness` run on a fixture (E18 shows clean runs work) with PREDICATES flipping false -> true ending at "goal objectively true"; capture as an asciinema cast + render an SVG/GIF. NEEDS A LIVE RUN (the operator or a live-capable session drives it; the headless pool likely cannot). This is kazi's benchmark-chart equivalent.  CONFIRMED LIVE BUG 2026-06-25: the CURRENT `proof-loop.svg` shows the REMOVED `kazi run my-goal.toml` verb on https://kazi.sire.run -- replacing the asset here also remedies that stale-verb bug (guarded going forward by T29.4).  Done 2026-06-28 (PR #751, released v1.67.0; recorded a REAL `kazi apply --harness claude` run on the committed fixture `priv/examples/hero_cast_demo/` -- a one-predicate `go test` goal converging in 2 iters [`failing=["tests-pass"]` -> `failing=[]` -> CONVERGED]; cast `assets/proof-loop.cast` + render `assets/proof-loop.gif`/`site/public/proof-loop.gif` replace the hand-drawn SVG mockups; README + site `index.astro` rewritten to match the real run; stale `kazi run` verb gone. Live-verified: https://kazi.sire.run serves the GIF [byte-identical SHA256], the old `/proof-loop.svg` 404s, README raw GIF 200, T29.4 verb guard green in BLOCKING mode).  Owner: TBD  Est: 2h  verifies: [UC-039]  delivers: [a REAL asciinema cast (+ rendered SVG) of the loop reaching objective-true]  deps: []  acc: the asset is a genuine recording of a real reconcile run (NOT a mockup), reused in README (above install) + site hero; the underlying cast file is committed so it is reproducible/verifiable; no stale `kazi run` verb remains in the rendered asset.
- [x] T25.3 README -- SINGLE canonical rewrite to the human->Claude->kazi->Claude spine + full capability coverage (supersedes T17.1; ABSORBS T22.1 coverage-map, T22.2 coverage-pass, T21.11 native-parallelism positioning -- one pass, not three): lead = tagline (T25.1) -> hero transcript (T25.2) -> a "without kazi / with kazi" before-after block (Context7 device) -> "you chat with Claude Code, it drives kazi" + the invocation phrase (T25.6) -> copy-paste wiring (the skill/`mcp` one-liner) -> the native-parallelism story ("kazi parallelizes your plan -- single machine, no NATS"; `/apply --pool` shown as interop) -> the token-economy note (T25.11, done) -> agent-native social-proof row. Then a COMPLETE, accurate capability+CLI reference (every shipped UC + `--json`/`status`/`schema`/`help --json`/`run --parallel`/`install-skill`/`mcp`/`propose`), with the raw `kazi` CLI clearly under "Reference (what the agent runs / advanced)" -- never the primary human path. Flip every "coming" tag to "available" (no vaporware). First produce the capability->location map (old T22.1) inline, then write to it.  Owner: TBD  Est: 2.5h  verifies: [UC-039, UC-035, UC-037, UC-034]  delivers: [a README whose first screen sells the agent-drives-kazi paradigm and whose body covers every shipped capability]  deps: [T25.2]  acc: the first screen shows the spine + hero + before-after; "a human runs the kazi CLI" appears only under Reference/Advanced; native parallelism + token economy are present; the full CLI is documented with no "coming" tags remaining; every command verified against `kazi help --json`; coherence (T9.9) green; renders on GitHub.
- [x] T25.4 Website -- SINGLE canonical rewrite to match the spine + OG card (supersedes T17.2; ABSORBS T22.5 coverage-pass + T25.9 OG/launch-kit -- one pass): hero = the transcript (T25.2) + the tagline; a primary "Chat with Claude Code, it drives kazi" section; without/with block; an agent-voiced testimonial (T25.5); the native-parallelism section + a feature overview covering the full shipped set; two-layer proof (heavier on the site); the raw kazi CLI demoted to a Reference/Advanced note. Produce the OG/Twitter card showing the agent paradigm (wire into `site/src/layouts/Layout.astro`) + the Show HN/X copy here. Update `canonical.mjs` + coherence; deploy + verify live.  Owner: TBD  Est: 3h  verifies: [UC-039, UC-035, UC-037]  delivers: [a website that leads with the agent paradigm, covers the full feature set, with an OG card + launch copy]  deps: [T25.3, T25.5]  acc: hero + primary section render the spine + hero asset; native parallelism + full feature set present; a human running the CLI appears only under Reference/Advanced; the OG card renders in a link-preview check; README<->site coherence (T9.9) green; Playwright smoke (T9.5) passes incl. mobile + the new sections; Lighthouse SEO >= 90; deployed + verified live at https://kazi.sire.run.
- [x] T25.5 Agent-voiced testimonial(s) (Serena pattern, uniquely on-brand): capture a coding agent describing -- in its own words -- what kazi lets it do (e.g. "I stop claiming done when it isn't"); HONEST + labelled as agent-authored. Use on README social-proof row + site.  Done 2026-06-25 (PR #454; one Claude-authored testimonial, labelled agent-authored; renders on README + site, verified live).  Owner: TBD  Est: 1h  verifies: [UC-039]  delivers: [1-2 agent-authored testimonials, labelled]  deps: []  acc: the testimonial is clearly attributed to the agent that produced it; not fabricated human quotes; renders on both surfaces.
- [x] T25.6 The invocation phrase -- DECIDED (operator 2026-06-24): the phrase is **"have kazi drive this until done"** (Context7 "use context7" pattern). POOL: document it identically across README/site/skill (`kazi install-skill` SKILL.md)/`AGENTS.md`, and ensure the skill trigger recognizes it so it actually drives kazi.  Done 2026-06-25 (PR #454; phrase identical across README/site/skill/AGENTS.md; trigger wired into the install-skill SKILL.md description so it routes once installed; T16.4 coherence green).  Owner: TBD  Est: 1h  verifies: [UC-039, UC-034]  delivers: [the decided invocation phrase, documented + wired]  deps: []  acc: "have kazi drive this until done" appears identically across README/site/skill/`AGENTS.md`; a real Claude Code session given the phrase drives kazi (or honest "coming" if the trigger is not wired yet); coherence (T16.4) green.
- [x] T25.7 Dogfood "done" leaderboard/gallery (the recurring growth engine): a page/section listing goals a prose pipeline left subtly broken that kazi converged -- built from the dogfood fixtures (T0.12/T1.8) + the live production probe -- with a REPRODUCIBLE methodology (the number must hold up; risk #1). Self-updating where feasible; each new fixture = a new entry.  Done 2026-06-26 (PR #745; new `/proof` gallery (`site/src/pages/proof.astro`) + `docs/dogfood-methodology.md`. THREE real converged `kazi apply --json` cases, numbers verbatim from docs/devlog.md: exact-content VERSION.txt (2 iters/18.5s/$0.116, v1.64.2, T26.6); sha256-oracle self-correction solution.py->610 (2 iters/39.3s/$0.0769, Haiku self-corrected iter 2, v1.64.1, T30.4); parallel needs-DAG streaming-consumes-contract (collective converged, 2 concurrent+1 gated, NATS-free, v1.64.2, T21.12/T23.9). Plus the regression-catch discipline exhibit (`mix test test/kazi/slice1_dogfood_test.exs`, labelled a test). Data-driven `cases` array (each new fixture = one entry). Linked from README (header + Proof section table) + site (Proof nav/footer on home+blog) + docs index. npm run build OK; Playwright 116 passed incl. 4 new assertions; coherence + command-accuracy + leak-guard green. Live verify at https://kazi.sire.run/proof is the post-deploy step (Pages deploys on merge).)  Owner: TBD  Est: 2h  verifies: [UC-039]  delivers: [a dogfood "done" gallery/leaderboard page + methodology]  deps: []  acc: the page shows >=2 real converged cases with before/after evidence + a reproducible method; no unverifiable claims; linked from README + site.
- [x] T25.8 Docs quickstart-first (tutorial-then-reference): the first `docs/` page is a Quickstart that wires kazi into Claude Code (`install-skill`/`mcp`) and converges ONE real goal end-to-end via the agent; reference (predicate DSL, budget/stuck, `--json` schemas) follows. Cross-linked from a `docs/` index.  Done 2026-06-25 (PR #459; `docs/quickstart.md` + new `docs/README.md` index, tutorial-then-reference; invocation phrase consistent with T25.6; only real verbs (`kazi plan`/`apply`/`install-skill`), also fixed `kazi mcp`/`adopt` drift in existing docs; docs-fresh + leak gates green).  Owner: TBD  Est: 1.5h  verifies: [UC-039, UC-033]  delivers: [an agent-first Quickstart as the docs entry page]  deps: [T25.6]  acc: a reader follows the Quickstart and drives kazi from Claude Code end-to-end on the current release; reference pages follow; only real commands.
- [x] T25.9 docs/ guides + `concept.md` -- SINGLE alignment pass to the spine + final architecture (ABSORBS T22.3 concept update, T22.4 guide-set, T22.6 presentation decision, T20.10 pool guide, T31.8 self-maintaining-docs positioning -- one pass): (a) `docs/concept.md` reflects the human->Claude->kazi->Claude spine, the NATIVE parallel scheduler (E21/ADR-0027, superseding the "external launcher only" framing), the agent-drivable+self-teaching stack (E15/E16), the `/apply --pool` interop (E20/ADR-0026), and the self-maintaining-docs lifecycle (E31/ADR-0036); (b) the `docs/` guide set is complete, accurate, and cross-linked from `docs/README.md` -- orchestrator recipe (T15.8), install-skill (T16.2), AGENTS.md (T16.3), the `/apply --pool` "harden with kazi" guide (4 layers, `/claim`<->lease boundary, failure modes), the native-parallelism guide, the JSON schemas; (c) every guide leads with the agent paradigm, raw CLI as the agent/advanced reference; (d) record the docs-presentation decision (repo-only vs a rendered `/docs` on the site).  Done 2026-06-25 (PR #624; concept.md gained §8b `/apply --pool` interop (ADR-0026), §10a self-maintaining-docs lifecycle (ADR-0036), §10b the recorded repo-is-source-of-truth presentation decision; corrected the dead `kazi watch` ref; cross-linked AGENTS.md from docs/README.md; docs/ index has no orphans; commands checked vs lib/kazi/cli.ex @commands).  Owner: TBD  Est: 2.5h  verifies: [UC-033, UC-035, UC-036, UC-037, UC-046]  delivers: [an aligned concept.md + a complete, indexed docs/ guide set on the spine]  deps: [T25.3]  acc: concept.md describes the native scheduler + spine + doc-lifecycle without contradicting ADR-0001; a `docs/` index links every guide; each guide references only real commands (checked vs `kazi help --json`); no orphan/missing guide for a shipped capability; the presentation decision is recorded.
- [ ] T25.10 Accuracy/coherence/publish + launch -- the SINGLE no-vaporware gate (ABSORBS T22.7 audit, T30.5 tiering-id gate, T22.8 launch checklist, T22.9 announcement): lean on the AUTOMATED checks first -- the E31 doc-freshness predicates (T31.4/T31.5) + the E29 CI guard already verify command-accuracy/coherence/freshness in CI, and the tiering accuracy/coherence gate absorbed from T30.5 (model-id + command + README<->site/AGENTS coherence over the tiering surfaces) is now AUTOMATED in CI too (delivered PR #580, 2026-06-25) -- then add the manual spot-checks they don't cover: every model id across skill/`AGENTS.md`/README/site verified against the claude-api reference (real current ids only -- Opus 4.8 / Sonnet 4.6 / Haiku 4.5), README<->site (T9.9) + skill/`AGENTS.md` (T16.4) coherence green, version current, no dead links, the proof-of-convergence visual matches real output, no unproven cost figure stated as measured, every "coming" flipped. Then the launch: a final checklist + publish (merge -> deploy -> verify live at https://kazi.sire.run + README renders on GitHub) + a short launch announcement/social copy (HN/X) leading with the spine.  Owner: TBD  Est: 2h  verifies: [UC-039, UC-045, UC-035, infrastructure]  delivers: [a green accuracy/coherence gate + the live publish + a launch announcement draft]  deps: [T25.3, T25.4, T25.7, T25.9]  acc: the automated freshness/coherence checks pass; zero unshipped-command or stale-model-id references; no dead links; current version; the live site shows the spine; README renders on GitHub; the announcement leads with human->Claude->kazi->Claude and every claim matches the shipped product; any skipped item flagged, not hidden.
- [x] T25.11 "Token economy without local models" content (ADR-0033, the broad-appeal cost story): a README/site section + a worked example showing the in-family Claude tiering -- you chat with Claude Code, it drives kazi, EASY iterations run on a cheap Claude model (e.g. Haiku 4.5), HARD reasoning on a frontier model (e.g. Opus 4.8), and predicates keep the cheap model honest -- so any Claude Code user gets better token economy with NO local model / local GPU host. Frame local/BYOM (opencode) as the secondary PRIVACY option, and reference the ADAPTIVE escalation recipe (ADR-0035/E30: start cheap, escalate on stuck) as the smart default. HONEST: the cost number is "designed for / being measured" until T19.7 runs (no unproven figure); model ids checked against the claude-api reference.  Owner: TBD  Est: 1.5h  verifies: [UC-043, UC-045, UC-039]  delivers: [a "token economy without local models" section + a worked frontier->cheap-Claude example + a pointer to the escalation recipe]  deps: [T19.6, T30.1]  acc: README + site show the in-family tiering example (`kazi plan` with a frontier model -> `kazi apply --harness claude --model <cheap>`) and mention the escalate-on-stuck behavior; local/BYOM is the secondary privacy note; no unproven cost number stated; commands verified against `kazi help --json`; coherence (T9.9) green.  Done 2026-06-25 (PR #575; README + site "Token economy without local models" section: in-family Claude tiering (chat with Claude Code -> drives kazi -> easy iters on a cheap model e.g. Haiku 4.5, hard reasoning on a frontier model e.g. Opus 4.8, predicates keep the cheap model honest) + worked `kazi plan` frontier -> `kazi apply --harness claude --model <cheap>` example + escalate-on-stuck pointer; local/BYOM framed as the secondary privacy option; NO unproven cost number (being measured until T19.7); commands verified vs `kazi help --json`, coherence green. Deployed + VERIFIED LIVE at https://kazi.sire.run (root 200, section present in live HTML, Pages deploy run success 07:58:41Z).
- [x] T25.12 Community + getting-help links (closes the all-stars.md growth-playbook gap #10, the last unaddressed item): the only fast-growing-OSS pattern still missing from kazi's surfaces. (a) Enable GitHub Discussions on `kazi-org/kazi` (a repo setting); if Discussions stays off, fall back to Issues. (b) README: add a one-line footer pointer -- "Questions? Start a [GitHub Discussion](.../discussions) | Read [concept.md](docs/concept.md) for the architecture". (c) Site: add a "Docs" (or "Community") nav link in `site/src/pages/index.astro` -- point it at `docs/concept.md` on GitHub until a rendered `/docs` exists (the T22.6 decision), plus a help link in the footer. Keep README<->site coherence (T9.9) green; update the Playwright smoke (T9.5) to assert the new nav link.  Owner: TBD  Est: 1h  verifies: [UC-039, UC-035]  delivers: [a Discussions/help link in the README footer + a Docs/Community nav + footer link on the site]  deps: []  acc: Discussions enabled (or an Issues fallback documented); README footer links to Discussions (or Issues) + concept.md; the site nav shows a Docs/Community link and the footer a help link, all resolving (HTTP 200); the Playwright smoke (T9.5) covers the new nav link; coherence (T9.9) green; deployed + verified live at https://kazi.sire.run.  Done 2026-06-25 (PR #467; README "Community & help" footer + site Docs nav + Discussions footer link; GitHub Discussions enabled; Pages deploy run 28148420990 success; verified live: root 200, Docs->concept.md 200, Discussions 200; Playwright 18 passed incl. 2 new assertions, T9.9 coherence green).
