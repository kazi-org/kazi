# E58 -- Bus reliability: version-skew dead-letters + CLI discoverability (P1)

Back-link: [docs/plan.md](../plan.md) (master index). Sibling epics: [plans/](.)

fidelity: executable

Two real bus bugs found via a full open-issue triage pass (2026-07-17). Both
are trust bugs in the E51/E55 session-bus surface (already in daily
production use across two machines): one is a silent, ASYMMETRIC failure
(writes succeed, reads fail) that makes a directed inbox message
disappear without any error surfacing to the sender -- the worst possible
failure shape for a coordination primitive, because it trains operators to
trust an ack that lied. The other is a CLI usability gap (no in-CLI way to
discover `bus post`'s required `<kind>` argument or valid kind values,
plus housekeeping log lines polluting stdout) that has already cost a
reporter real trial-and-error time.

Acceptance: a CLI/daemon version-skew condition either fails loud on write
too (not just read) or is detected and reported before it can produce a
dead-lettered message; `kazi bus post` without `<kind>` either defaults
sensibly or fails with a usage line enumerating valid kinds; every `bus`
subcommand has real per-verb `--help` text; `kazi bus read` stdout carries
only message payloads, never housekeeping log lines.

## Workstream A -- version-skew dead-letter (#1227)

- [x] T58.1 Investigate the bus RPC/version-negotiation path for the read/write asymmetry (#1227): a long-running bus daemon (started under an older kazi) kept accepting `bus join`/`who`/`tell`/inbox-accumulation from a newer CLI (1.172.0), but `bus read`/`bus read --peek` from that same newer CLI failed with `error: daemon could not read the bus: unknown_op`. Net effect: `who` showed the session with a nonzero inbox count, senders got a successful `told <name>` ack, but the recipient could never read any of it -- a silent dead-letter queue where senders believe delivery succeeded. Read the daemon<->CLI RPC dispatch code (the `unknown_op` error implies an operation-code mismatch between CLI and daemon protocol versions) and determine WHY writes succeed but reads fail under this skew -- is it that the read path added a new op code after the write paths were already stable, or something else. Write findings to docs/devlog.md before scoping the fix in T58.2 (this task is investigation-only; do not fix yet, the fix approach depends on the root cause).  Owner: pool  Done: 2026-07-17  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: docs/devlog.md carries a dated root-cause entry identifying exactly which op codes/protocol versions are involved and why the failure is asymmetric (write succeeds, read fails) rather than symmetric; the entry names the specific fix direction for T58.2 (daemon rejects skewed writes too, OR CLI detects skew proactively, OR both) with justification. Done: 2026-07-17 (PR #1371; root cause: `join`/`who`/`tell` publish direct-to-NATS and never touch the daemon's Elixir op-dispatch code, so an old daemon binary can't break them, while `read` is a T55.7/ADR-0072-d5 addition dispatched through the daemon's control-socket `Kazi.Daemon.Control.handle/2`, so an old daemon falls through to the `unknown_op` catch-all -- structural asymmetry, not a bug in either path alone; recommends T58.2 do CLI-side proactive skew detection at connect via `ping`'s `vsn`, extending T52.2's `schema_vsn` handshake; see devlog "T58.1: root-caused the bus read/write version-skew asymmetry").
- [ ] T58.2 Fix the version-skew asymmetry per T58.1's finding (#1227): implement whichever fix direction T58.1 recommends. If "daemon should reject the mismatched-version write path too": the daemon must fail LOUD (a clear error to the sender, not a silent accept) on any op it cannot fully service under the negotiated version, symmetric with how reads already fail. If "CLI should detect skew and prompt an upgrade": add a version-check at connect time that surfaces a clear "daemon is running an older/incompatible version, restart it" message before any operation is attempted, so the failure happens at the FIRST touch point, not silently mid-session. Either way, the end state must be: no operation can ever appear to succeed (an ack) while silently not doing what it claims. COORDINATE WITH E52: T52.2 is already adding a version handshake to the SAME daemon (an additive `schema_vsn` in the control-socket `ping` reply + `SchemaSkew.classify/2` for the read-model write path) -- if T58.1's finding points at connect-time skew detection, EXTEND that handshake (a bus-protocol version alongside `schema_vsn`) rather than inventing a second, parallel version-negotiation mechanism on one daemon; two independent skew checks with different semantics is itself a future #1227.  Owner: pool  Est: 3h  kind: agent  verifies: [infrastructure]  deps: [T58.1]  acc: ExUnit with an injected version-skew fixture (mock daemon reporting an older protocol version) -- the previously-silent-success write path now either fails loud with a clear error, or is preceded by an explicit skew-detection error before any write is attempted; a symmetric fixture confirms reads and writes fail (or succeed) consistently under the same skew condition, no more read/write asymmetry; docs/session-bus.md documents the skew-handling behavior.

## Workstream B -- bus CLI discoverability (#1060)

- [x] T58.3 `kazi bus post` kind UX + per-verb help + clean stdout (#1060): three related gaps in the same issue thread. (1) `kazi bus post "some text"` (without the required `<kind>` positional arg) fails with an unhelpful error that does not list valid kinds -- either default `kind` to `fact` when omitted, or fail with a usage line enumerating every valid kind (pick whichever preserves the existing explicit-kind behavior for callers who already pass one; do not silently change behavior for existing correct callers). (2) `kazi bus post/read/tell/who --help` (and other `bus` subcommands) currently print the generic top-level `kazi` usage block instead of subcommand-specific usage -- add real per-verb `--help` text for every `bus` subcommand, following whatever per-command help-text convention the rest of the CLI already uses (check `lib/kazi/cli.ex`'s `@flag_docs`/command help pattern -- this repo has a known landmine where a new flag/command needs registering in FOUR places: `@switches`, the command's flag list, the help TEXT, and `@flag_docs`, or `help --json` crashes; run the help-schema/coherence test suites before pushing regardless of whether this task touches flags). (3) `kazi bus read` currently intermixes housekeeping log lines (e.g. "Skipped cleanup of older version (vX.Y.Z): still in use by a running process") with message payloads on stdout, which breaks machine parsing of `bus read`'s output -- route housekeeping/log lines to stderr so stdout stays clean JSON/text for scripting.  Owner: pool  Done: 2026-07-17  Est: 2.5h  kind: agent  verifies: [infrastructure]  deps: []  acc: `kazi bus post "text"` (no kind) either succeeds with kind=fact or fails with a usage line listing valid kinds (pick one, pin it as a regression test); `kazi bus post --help` (and read/tell/who --help) print subcommand-specific usage, not the generic top-level block; `mix test test/kazi/cli_help_schema_test.exs test/kazi/teach_coherence_test.exs test/kazi/teach_coherence_reverse_test.exs test/kazi/cli_self_conformance_test.exs` all green; `kazi bus read`'s stdout contains zero housekeeping/log lines in a fixture that forces a cleanup-skip during the read (log lines appear on stderr instead, verified by capturing streams separately in the test).
- [ ] T58.4 Route the burrito wrapper's housekeeping lines off STDOUT (#1060 "Also", follow-on to T58.3): T58.3 fixed `bus read`'s stdout hygiene at the Elixir-logger layer (`config :logger, :default_handler, config: [type: :standard_error]`), but a SEPARATE, earlier code path still pollutes stdout: the burrito zig wrapper (`kazi-org/burrito` fork, pinned in `mix.lock`, ADR-0066/#1018) prints its own housekeeping to STDOUT before the BEAM even starts -- `[i] New install path is: <dir>` on first-invocation extraction and `[l] Skipped cleanup of older version (vX.Y.Z): still in use by a running process` during old-version cleanup. Live-observed on the v1.195.0/v1.212.0 release binaries during the T59.2 mixed-version repro (every invocation emitted `[i] New install path is: …` to stdout). Because it is pre-BEAM zig output, no Elixir-side logger change can reach it, so `kazi version --json` (and any machine-parsed stdout on a fresh-install / post-upgrade invocation) is polluted. Fix in the burrito fork: emit these `[i]`/`[l]` maintenance/launcher lines to STDERR (or gate them behind a verbosity flag), then bump the `mix.lock` burrito pin to the fixed fork commit. Cross-repo change (kazi-org/burrito + a mix.lock bump in this repo); file/land the burrito change first. Owner: pool  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: a fresh-install (or post-upgrade) invocation of the released binary prints ZERO `[i]`/`[l]` wrapper lines to stdout -- stdout carries only the command's own output (e.g. `kazi version` prints exactly the version string, `kazi version --json` is parseable JSON with no leading `[i]` line), the wrapper lines appear on stderr instead; verified by capturing stdout and stderr separately against a built release binary in a scratch install dir (the T59.2 method: `HOME=<scratch>` + per-version `KAZI_INSTALL_DIR`, forcing a first-invocation extraction); docs note the wrapper-output stream contract.

## Wave plan

T58.1 (investigation) blocks T58.2 (fix) -- sequential. T58.3 is independent
(different subsystem, CLI-layer UX vs. daemon-protocol correctness) and can
dispatch in the same wave as T58.1.

## Risk register

| id | risk | likelihood | impact | mitigation |
|---|---|---|---|---|
| R-E58-1 | T58.2's fix direction changes daemon/CLI wire compatibility in a way that breaks an already-running daemon on an older version mid-upgrade. | Med | Med | The acc line requires a version-skew fixture test BEFORE the fix ships; additionally, verify the fix's behavior against a daemon one full minor version behind, not just the exact skew reported in #1227. |
