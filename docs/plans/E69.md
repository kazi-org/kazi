# E69 -- Triage 2026-08-08: full open-backlog resolution -- replace/apply false green, #1255-class startup wedges, daemon crash-loop, DX contract gaps, accepted enhancements (#1679, #1683, #1680, #1662, #1684, #1685, #1483, #1678, #1681, #1617, #1682, #1642, #1649, #1636)

Back-link: [docs/plan.md](../plan.md) (master index). Sibling epics: [plans/](.)

fidelity: executable

The 2026-08-08 full open-issue triage pass. 16 issues were open; all were
labeled and cross-linked during triage, and the two maintainer decisions were
cleared (#1682 hook ACCEPTED as proposed; #1642 DECIDED as option 2 --
explicit setup step in the goal-file). #382/#372 already ride T25.10 (E25,
the last remaining E25 task) and stay there. The remaining 14 issues fall
into four families (13 now, per the 2026-09-02 refinement note below --
#1683/T69.2 closed 2026-08-31):

1. **P0 correctness/availability on v1.275.0** (#1679 apply converges against
   the pre-`--replace` predicate set -- a false green, the one outcome the
   controller must never produce; #1683 apply wedges at startup with the
   #1255 idle-scheduler signature, 4/4 repro, plan/approve/`--check`
   unaffected; #1680 `apply --json` exits non-zero with EMPTY stdout and
   orphans its harness child -- likely the same wedge as #1683 but with its
   own contract obligations; #1662 the startup watchdog manufactures false
   hang diagnoses during normal long waits, polluting every #1255-class
   report above).
2. **Daemon/dashboard reliability** (#1684 an orphaned `nats-server`
   surviving its dead parent squats the port and puts a new daemon into a
   silent launchd crash-restart loop -- 8888 crash lines in one 24MB log;
   #1685 `kazi dashboard` startup blocks in `SwapDiagnosis.guard/
   serve_dashboard` `Process.sleep` up to fully unresponsive on long-uptime
   daemons; #1483 the mission-control mount query scales with total run
   count -- "a restart only buys ~an hour". #1685 specimen 1 and #1483's
   residual are probably one root cause; the instrumentation task decides.)
3. **DX / contract gaps** (#1678 opaque "could not create an isolated task
   worktree" + the undocumented workspace-derivation contract that burned a
   live wave twice; #1681 `portfolio --json` emitted+versioned but absent
   from `Kazi.CLI.Schema`; #1617 `--project` flag-doc mislabel).
4. **Accepted enhancements + test-integrity debt** (#1682 post-disposition
   command hook, accepted; #1642 explicit goal-file setup step, decided;
   #1649 bus discovery unreachable from the suite by construction; #1636
   the self-conformance fixture blows its own 15s contract on loaded boxes).

Code anchors verified on origin/main @ a6aaca4c: `Kazi.SwapDiagnosis.guard/2`
(lib/kazi/swap_diagnosis.ex:64), the NATS stop tuple (lib/kazi/daemon/
nats.ex:184), `execute_approve`/`execute_reject` (lib/kazi/cli.ex:8130/8229),
`@schemas`/`@ordered_commands` (lib/kazi/cli/schema.ex:35/419, no portfolio
key), the discovery-test fixture (test/kazi/bus/remote_peer_discovery_test.
exs:41), `converging_run/0` (test/kazi/cli_self_conformance_test.exs:541).
Next ADR number at plan time: 0084.

Acceptance: every issue above is closed citing a merged PR (or an explicit
recorded disposition on the issue); the #1679 fix is proven by a red->green
replay of the exact repro; no fix weakens an existing assertion; docs land
with the code (ADR-0034); fixes reaching a release are verified on the
RELEASED binary where the symptom was release-binary-specific (#1683, #1684,
#1685).

## Tasks

- [ ] T69.1 Fix #1679 -- apply executes a superseded predicate set after `plan --replace` + `approve` (FALSE GREEN, worst-in-class): `kazi status` throughout showed the pre-replace run (`in_progress`, `iteration: 0`), so apply short-circuited against the stale run record instead of loading the re-approved proposal's 9-predicate vector. Fix: at dispatch, apply loads the CURRENTLY-approved proposal's predicate set; if a live/duplicate run record exists whose predicate set differs from the approved proposal, either supersede it (fresh observation against the new set) or refuse with a clear error -- silently converging on the old set must be impossible. Pin with the exact repro as a fixture: plan -> approve -> apply -> converged; tighten via plan --replace -> approve -> apply must dispatch (or refuse), never report `collective: converged` while a replaced predicate is red.  Owner: pool  Est: 4h  kind: agent  verifies: [infrastructure]  deps: []  acc: [fixture replay of plan/approve/apply then plan --replace (tightened set)/approve/apply never emits collective converged while a replaced predicate fails -- it dispatches against the new set or refuses loudly; red on pre-fix main]
- [x] T69.2 Root-cause + fix #1683 -- `kazi apply` wedges at startup with the #1255 idle-scheduler signature on v1.275.0 (4/4 repro incl. solo runs; plan/approve/`--check` unaffected; process sits alive indefinitely post-watchdog, only SIGTERM clears it). The watchdog dump tops out at `Kazi.Release.burrito_main/0` with all-zero run queues. Find the unbounded receive on the apply/scheduler startup path (the #1266 bus-deadline treatment covered CLI first-invocation; this class is still reachable from apply startup), bound it with a deadline that degrades to a LOUD error exit, and pin the unblocked path.  Owner: pool  Est: 6h  kind: agent  lane: agent  verifies: [infrastructure]  deps: []  acc: [the blocking site on the apply startup path identified from #1683's dump carries a bounded wait that exits loudly instead of wedging (ExUnit-pinned red on pre-fix main), and a fixture apply on the release binary reaches first observation without tripping the watchdog]  Done: 2026-08-31 (PR #1710 "fix(runtime): bound the apply startup wait so a wedged first observation exits loudly (#1683)", regression pinned in a follow-up commit, released v1.275.2 via PR #1711; #1683 closed 2026-08-31T19:48:51Z. Landed independently of an /apply --pool dispatch against this epic -- caught by this /plan refinement pass as a plan/reality drift: the task sat unticked for 24h after its own fix and release merged.)
- [ ] T69.3 Fix #1680 -- `apply --json` terminal-result contract + child reaping: every exit path emits exactly ONE terminal JSON result object on stdout (internal failure => `{"status":"error","next_action":"investigate",...}`, never an empty stream); a controller exiting non-zero reaps or explicitly hands off its harness child (at minimum prints the orphaned pid + `kazi orphans --reap` pointer); add a `--goal <ref>` filter to `kazi orphans --reap` so one lane can reap without touching a sibling lane's runs. Re-verify the original repro after T69.2 lands (shared root cause suspected; contract fix is independent).  Owner: pool  Est: 3h  kind: agent  verifies: [infrastructure]  deps: []  acc: [a forced internal apply failure in a fixture yields one parseable terminal JSON object with status error on stdout and no silently surviving harness child (reaped or an explicit orphan-pid handoff line), and orphans --reap --goal <ref> reaps only that goal's orphans (pinned)]
- [ ] T69.4 Fix #1662 -- the startup watchdog manufactures false hang diagnoses: it printed the "#1255 signature" verdict while blocked in `System.do_cmd` (harness dispatch during `kazi plan`) and `Loop.await/2` (controller awaiting an iteration) -- both normal multi-minute waits. Classify known-legitimate wait MFAs (harness dispatch via `CliAdapter.do_dispatch`, `Loop.await`, `serve_dashboard`'s serve sleep) and for those either suppress the banner or emit a neutral "waiting on <X> (expected)" line WITHOUT the idle-scheduler-receive-block conclusion; the full diagnostic stays for unrecognized blocked sites. This de-noises every other #1255-class investigation (T69.2, T69.6).  Owner: pool  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: [watchdog firing against a process blocked in a known-wait MFA emits no #1255-signature verdict (pinned per known site), while an unknown blocked MFA still gets the full diagnostic (pinned)]
- [ ] T69.5 Fix #1684 -- orphaned `nats-server` blocks the new daemon's bind => silent crash-restart loop under launchd (`{:nats_server_exited, 1}` at lib/kazi/daemon/nats.ex:184 is fatal to the whole app; 8888 crash lines observed). Three-part fix per the issue: (a) on bind failure, probe the configured port -- if a compatible NATS/JetStream instance already serves it, adopt/reuse it or reap the orphan (match `-sd <jetstream dir>` + port, only when no live kazi daemon owns it) before respawn; (b) log a DISTINCT greppable error (`nats bind conflict: port <n> held by pid <p>, not ours`) instead of the generic GenServer terminating; (c) N nats-exits within a short window surfaces a `restart_loop`/bind-conflict flag in `kazi daemon status` (human + --json) instead of looping silently.  Owner: pool  Est: 4h  kind: agent  verifies: [infrastructure]  deps: []  acc: [with a squatter on the configured NATS port, daemon start logs the distinct bind-conflict line naming port+pid and does not enter a crash loop (adopts, reaps, or degrades once -- pinned), and daemon status --json carries the restart-loop/bind-conflict signal in that state]
- [ ] T69.6 Fix #1685 -- bound the dashboard startup guard: `Kazi.CLI.serve_dashboard/4` under `Kazi.SwapDiagnosis.guard/2` blocks in `Process.sleep/1` while HTTP requests are accepted but never answered (fresh restart: ~3 min to first 200; 9-day specimen: wedged indefinitely). Give the serve path the #1266 treatment -- the listener answers immediately with an explicit "starting, retry shortly" response (or a bounded-degrade page) while warmup proceeds, so the socket is never accept-but-silent. ALSO instrument what the guard/first-query path actually waits on and whether it scales with `~/.kazi/runs/` count or DB/WAL size (specimen 1: 8MB db + 4MB WAL + ~36k runs/) -- the finding feeds T69.7 and decides whether #1685+#1483 are one root cause.  Owner: pool  Est: 3h  kind: agent  verifies: [infrastructure]  deps: []  acc: [a freshly started dashboard answers HTTP within 10s of binding -- 200 or an explicit starting/degraded response, never accept-then-silence -- on a fixture seeded with a large runs/ population (pinned), and the warmup wait's dependence (or not) on runs//WAL size is recorded on the issue]
- [ ] T69.7 Fix #1483 residual -- the mission-control mount query scales with total run history (fast after restart, degrades over hours; "a restart only buys ~an hour"): bound/paginate the fleet query behind `MissionControlLive.assign_fleet/1` (LIMIT + recency window, never a full-history scan on mount) and stop `RunReaperTicker` monopolizing the connection (shorter reaper transactions and/or a dedicated read path), per the diagnosis already recorded on the issue. Verify against a read-model seeded with tens of thousands of runs.  Owner: pool  Est: 4h  kind: agent  verifies: [infrastructure]  deps: []  acc: [assign_fleet on a read-model seeded with >=20k runs executes a bounded query (LIMIT/pagination pinned in a test) and completes within its budget with a concurrent reaper transaction running (pinned); #1483 closed citing the PR after a soak on a large-history daemon]
- [ ] T69.8 Fix #1678 -- workspace-derivation contract + opaque worktree error: (a) when isolated-worktree creation fails, surface the underlying git error and at least one concrete remedy (name the branch-already-checked-out conflict; point at `--in-place`, deleting the pre-created worktree, or a different goal id); (b) document the derivation contract -- `--workspace` is a BASE that serial AND `--parallel` modes derive isolated worktrees from (`$TMPDIR/kazi-worktrees/p-<hash>`, `kazi-partition/*`/`task/<goal-id>` branches), `--check` runs predicates IN the given workspace, `--in-place` is the run-here flag -- in `kazi help`, the orchestrator recipe, and an AUTHORING.md note warning that environment-identity guards pinning workspace PATHS are structurally unsatisfiable under isolation (use repo-marker assertions).  Owner: pool  Est: 2.5h  kind: agent  verifies: [infrastructure]  deps: []  acc: [the worktree-creation failure message names the underlying git cause and a concrete remedy (pinned), and the docs state where predicates run in --check vs serial vs --parallel including the branch naming, verified by the doc-freshness checks]
- [x] T69.9 Fix #1681 -- register `portfolio` in `Kazi.CLI.Schema`: add the `"portfolio"` entry to `@schemas` + `@ordered_commands` (lib/kazi/cli/schema.ex) in the same {field, type, description}+example shape as the existing four, matching `portfolio_json/1`'s emitted keys (schema_version stays 2 -- documents an existing shape); add `docs/schemas/portfolio.md` mirroring it; update the moduledoc's valid-keys list.  Owner: pool  Est: 1h  kind: agent  verifies: [infrastructure]  deps: []  acc: [kazi schema portfolio resolves and its fields match portfolio_json/1's emitted keys (pinned by a conformance test), docs/schemas/portfolio.md exists, no schema_version bump]  Done: 2026-09-05 (PR #1740, released v1.277.0. Plan/reality drift: the task sat unticked after its own PR merged.)
- [x] T69.10 Fix #1617 -- `--project` @flag_docs mislabel: the flag doc says "bus who only" but `kazi plan --project` also consumes it; correct the registry text in lib/kazi/cli.ex (remember the 4-place flag registration when touching flags).  Owner: pool  Est: 0.5h  kind: agent  verifies: [infrastructure]  deps: []  acc: [help --json's --project flag doc names both the bus who and plan --project usages, pinned by the help-schema test]  Done: 2026-09-05 (PR #1741, released v1.276.1. Plan/reality drift: the task sat unticked after its own PR merged.)
- [ ] T69.11 Implement #1682 (ACCEPTED 2026-08-08) -- `[hooks] post_disposition` command hook: a configured command invoked SYNCHRONOUSLY after a successful approve/reject transition persists, as `<command> <proposal-ref> <approved|rejected>`; kazi neither parses its output nor branches on it; non-zero is reported without undoing the disposition (the execute_approve optional-file-write precedent at lib/kazi/cli.ex:8157-8167). Insertion points: `execute_approve/2` (:8130) and `execute_reject/2` (:8229). Docs land with the code (config reference + a note that the bus is NOT the disposition channel, per the issue's own analysis).  Owner: pool  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: [a configured post_disposition hook receives ref+state after both approve and reject in a fixture (pinned), a failing hook leaves the disposition intact and reports non-zero without pretending the disposition failed (pinned), and the hook is documented]
- [ ] T69.12 ADR + implement #1642 (DECIDED 2026-08-08: option 2 -- explicit setup step in the goal-file): mix-backed predicates red at t0 for environmental reasons (no deps in a fresh task worktree) weaken the red-at-t0 signal. Add a declared setup/provisioning step to the goal-file schema (e.g. `[setup]` command block) that the worktree/preflight path runs BEFORE the t0 observation; a setup failure is a distinct environment error, never a predicate red; auto-detection and harness-provisions-implicitly are explicitly out (recorded on the issue). ADR first (goal-file surface + t0 semantics; next number was reserved
~0084 at plan time, but ADR-0084 was taken by T70.13/#1705 -- 2026-09-02
re-check: ADR-0085 is also taken (T70.9); this task must `ls docs/adr/`
fresh at execution time and take the next free number, 0086 at time of
writing). Docs: goal-file schema reference + AUTHORING.md note that build-tool-backed predicates require a declared setup step, with red-at-t0's meaning restored.  Owner: pool  Est: 4h  kind: agent  verifies: [infrastructure]  deps: []  acc: [accepted ADR; a fixture goal with mix-backed predicates and a declared setup step reaches t0 with deps provisioned so predicate red is a product signal (pinned), setup failure surfaces as a distinct non-predicate error (pinned), and the schema + AUTHORING docs cover it]
- [ ] T69.13 Fix #1649 -- bus discovery + connection establishment reachable from the suite: promote the #1644 fixture (`remote_peer_daemon`/`Kazi.TestSupport.BusPeer`) into shared test/support; add at least one test PER bus verb that routes through REAL discovery (no injected `opts[:conn]`) against it; cover BOTH peer states from #1606 (refusing => fast error; blackholing => blocks to the bound) on RFC 5737 TEST-NET addresses so tests reproduce anywhere. Read first-run failures as discovery, not regression (the issue predicts the sweep may surface more live defects -- file fresh issues for any found, do not silently patch).  Owner: pool  Est: 3h  kind: agent  verifies: [infrastructure]  deps: []  acc: [a shared test/support bus-peer fixture exists and every bus verb has at least one suite test exercising real discovery with no injected conn, covering refuse and blackhole peer states on TEST-NET addresses]
- [x] T69.14 Fix #1636 -- `converging_run/0` exceeds its own 15s await_timeout on loaded boxes: make the fixture CHEAPER (the issue rules out another probe-bound bump as dishonest -- the run blows its own contract). Profile where the cost lives (real loop + inets httpd + deploy/integrate stubs; `reobserve_interval_ms: 5` already set) and cut it. Landmine from the issue: pin the test by NAME, never by line number, when re-measuring.  Owner: pool  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: [the apply self-conformance fixture run completes within its declared 15s await_timeout in 5/5 consecutive local runs under parallel load with the probe bound unchanged, and CI stays green]  Done: 2026-09-05 (PR #1754, merged 16:00 UTC. Plan/reality drift: not yet in a tagged release as of this check -- main is unreleased since v1.279.1.)

## Waves

- **Wave E69-A (P0 first, 3 agents remaining, independent):** T69.1, T69.3, T69.4 -- the false green, the terminal-result contract, the watchdog de-noise. T69.2 (the apply wedge) is DONE (PR #1710/#1711, 2026-08-31); T69.4 landing makes any future #1255-class diagnostics trustworthy.
- **Wave E69-B (reliability, 5 agents, independent):** T69.5, T69.6, T69.7, T69.8, T69.14.
- **Wave E69-C (surface + tests, 5 agents, independent):** T69.9, T69.10, T69.11, T69.12, T69.13.
- **Rides elsewhere:** #382/#372 close via T25.10 (plans/E25.md) -- a lane finishing E69 waves should claim T25.10 next so the backlog actually reaches zero.

## Notes

- 2026-09-02 (/plan refinement pass): T69.2/#1683 confirmed DONE -- closed on
  GitHub 2026-08-31T19:48:51Z via PR #1710 (fix) + a same-day pinning commit
  + PR #1711 (release v1.275.2). This epic's remaining 13 tasks (T69.1,
  T69.3, T69.5-T69.14) are still 0/13, entirely untouched since the
  2026-08-08 triage -- no `/apply --pool` wave against E69 has run yet.
- Triage disposition 2026-08-08: all 16 open issues labeled; #1680 cross-linked
  to #1683 (suspected shared root cause, distinct fix obligations); #1685
  cross-linked to #1483 (T69.6's instrumentation decides if they are one
  fix). Operator decisions recorded on the issues: #1682 accepted as
  proposed; #1642 decided as explicit-setup-in-goal-file (option 2).
- #1636 is being resolved by cheapening the fixture despite its own
  "promote only if CI reds" trigger, because the operator directive is to
  resolve the full backlog; the honest-fix constraint (no probe-bound bump)
  is preserved.
- Release-binary verification matters for this epic more than most: #1683,
  #1684, #1685 were all observed on the RELEASED burrito binary under
  launchd, where supervision and boot paths differ from `mix test`'s. Per
  the kazi definition of done, the local binary must be upgraded to the
  fix release and the symptom re-probed live.
