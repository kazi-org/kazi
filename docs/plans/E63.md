# E63 -- Dashboard UX: truthful attention, balanced widgets, legible goal views, operator questions (P1)

Back-link: [docs/plan.md](../plan.md) (master index). Sibling epics: [plans/](.)

fidelity: executable

Operator feedback (David, 2026-07-17, live on the v1.212.0 dashboard at
http://localhost:4050 immediately after the PR #1366 mission-control changes
shipped). Four concerns, verbatim intent:

1. **NEEDS ATTENTION shows stale/old runs** -- the section's purpose is unclear
   and its contents are untrustworthy. Root cause is known territory: the
   section is `Kazi.Attention.Queue.build/2` (`lib/kazi/attention/queue.ex:105`)
   ranked over registry runs, and the registry carries ghost `running` rows
   with days-stale heartbeats (#1155, E60/T60.2). An attention queue that
   ranks phantoms trains the operator to ignore it -- the worst outcome for
   an attention surface.
2. **The new widgets (PR #1366: project badge, age/last-active row,
   state/repo/time chip filters) feel unbalanced** against the original
   mission-control visual system. Wants design explorations to pick from,
   not a single take.
3. **The goal drill-in and history views are not user friendly** (e.g.
   `/goals/<goal>/drillin` and `/goals/<goal>/history`) -- it is unclear what
   they are supposed to show; history is harder still.
4. **The dashboard should answer the operator's four standing questions**:
   what is my fleet working on; where can I help; what is blocking them; how
   much longer until projects complete (explicitly acknowledged as the tricky
   one). Overlaps E60's portfolio/attention work (T60.3/T60.4) -- this epic
   must compose with it, not duplicate it.

Design-direction selection is HUMAN-GATED: David picks from presented
explorations before any implementation task builds the chosen direction.

Exit criteria: NEEDS ATTENTION contains only actionable, heartbeat-fresh
entries and states what qualifies an entry; David has selected a widget
design direction from real explorations and it is implemented; the drill-in
and history views each lead with a one-line answer to "what am I looking
at"; the dashboard answers the four operator questions with honest data
(no fabricated ETAs -- ADR-0046 honest-unknown applies to time estimates).

## Tasks

- [x] T63.1 Make NEEDS ATTENTION truthful and self-explanatory (#1155 surface half): gate `Kazi.Attention.Queue.build/2` input on heartbeat freshness (reuse `RunRegistry.stale?/2`'s existing window -- the same predicate the duplicate-run guard trusts; do NOT invent a second threshold) so ghost `running` rows never rank; alerts derived from terminal states older than the scope toggle's CURRENT window drop out by default. Add a header affordance stating what qualifies an entry (e.g. "stuck, over-budget, or awaiting operator -- live runs only") so the section explains itself. COORDINATE: E60/T60.2 fixes the registry rows themselves; this task makes the VIEW robust regardless (defense in depth, same rationale as R-E59-2) -- do not wait on T60.2, do not duplicate its reaper.  Owner: pool  Est: 2.5h  kind: agent  verifies: [UC-068]  deps: []  acc: ExUnit -- a fixture registry with one fresh-heartbeat stuck run, one 9-day-stale `running` ghost, and one old terminal run renders an attention queue containing ONLY the fresh stuck run; the section header names the qualifying conditions; existing attention-queue ranking tests pass unchanged for fresh entries.  Done: 2026-07-17
- [x] T63.2 Widget-balance design explorations (human-gated selection): produce 3-4 SELF-CONTAINED visual explorations for the mission-control card widgets (project badge, age/last-active row, chip filter row) that rebalance them against the original card system -- e.g. (a) restrained: badge and age demoted to a single muted metadata line; (b) integrated: filters merged into the fleet-count strip, cards unchanged from the original; (c) rework: a rebuilt card grid where provenance is first-class. Each exploration is a static HTML mock (real dashboard markup + CSS, seeded with realistic fleet data, light and dark) so David can open them side by side in a browser; present via AskUserQuestion with one option per direction. NO production code changes in this task.  Owner: pool  Est: 3h  kind: agent  lane: agent  verifies: [UC-061]  deps: []  acc: 3-4 static HTML explorations exist under a scratch/branch path, each rendering the same seeded fleet data in light and dark; David has been presented the options and a selected direction is recorded on this epic (T63.5 consumes it).  Done: 2026-07-17 -- 4 explorations (CURRENT baseline + A restrained / B integrated / C provenance-first) presented as a reviewed page; David selected **B -- INTEGRATED**: cards revert to the original anatomy, the grid groups by project under ruled headers (badge removed from cards), age compresses to one right-aligned relative timestamp, filters fold into the FLEET header as segmented controls. T63.5 implements this direction.
- [ ] T63.3 Goal drill-in + history legibility redesign proposal: for `/goals/:goal/drillin` (drillin_heatmap_live.ex) and `/goals/:goal/history` (history_live.ex), draft a redesign that (a) leads each view with a one-line purpose statement and a plain-language summary ("this goal converged in 4 iterations; 2 predicates went red->green"), (b) renames or annotates every axis/cell/color so a first-time viewer can decode it without reading source, (c) reorders history to newest-first narrative events (iteration N: dispatched -> predicates X passed -> verdict) instead of raw event rows. Deliver as an annotated mock (same static-HTML format as T63.2) plus a short gap list of data the views need but the read-model does not yet expose. NO production code changes; implementation is scoped by T63.5 after David reacts.  Owner: pool  Est: 3h  kind: agent  lane: agent  verifies: [UC-062]  deps: []  acc: annotated mocks for both views exist rendering a REAL goal's data shape (use the runtime-gherkin goal from the report as the fixture); each mock's header answers "what is this view for" in one sentence; the data-gap list names each missing field and its read-model source or absence.
- [ ] T63.4 Operator-questions information architecture (the brainstorm, made concrete): design how the dashboard answers the four standing questions -- (1) "what is my fleet working on" (fleet grid + T60.1 cross-machine facts), (2) "where can I help" (T60.3 attention fan-in; T63.1's truthful queue), (3) "what is blocking them" (blocked/stuck states with the BLOCKER named, not just counted), (4) "how long until done" -- the tricky one: NO fabricated ETAs (ADR-0046); instead show objective progress-rate data (predicates green vs total per goal, red->green velocity over recent iterations, budget consumed vs cap) and let the human extrapolate; label it as rate, never as a promised date. Also address "less intimidating": propose a default view that hides expert surfaces (DAG, event river, lease map) behind an "operator" / "debug" mode split. Deliverable is a written IA proposal (docs/ or the epic file) + one composite mock; it must map every element to an EXISTING objective source or name the gap (ADR-0011 read-only projection line), and reconcile explicitly with E60's T60.3/T60.4 so the two epics ship one coherent surface.  Owner: pool  Est: 3.5h  kind: agent  lane: agent  verifies: [UC-061, UC-068]  deps: []  acc: the IA proposal exists and every proposed element cites its objective data source (file anchor) or is listed as a gap; the four questions each have a named answering surface; the ETA section proposes only rate/progress displays with an explicit no-fabricated-dates rule; overlap with T60.3/T60.4 is reconciled in prose (who ships what).
- [ ] T63.5 PLAN: expand implementation to executable fidelity after David selects directions: once T63.2's selection is recorded and David has reacted to T63.3/T63.4 proposals, decompose the implementation (chosen widget direction, drillin/history rebuild, operator-mode IA) into 30-90 minute tasks with acc: lines and waves, including paired browser tests (the dashboard is user-facing UI: Playwright/agent-browser coverage of the golden path per surface, plus the T63.1 attention fixture as a live check) and the ADR if the operator/debug mode split proves to be a real architectural decision.  Owner: pool  Est: 2h  kind: plan  verifies: [UC-061]  deps: [T63.1, T63.2, T63.3, T63.4]  blocked-by: [T63.2]  acc: this epic re-parses at executable fidelity with the implementation tasks; David's selected direction is recorded; every UI task carries a paired browser-test criterion.

## Waves

- **Wave E63-A (parallel, all independent):** T63.1 (attention truthfulness -- the only production code change, small and testable), T63.2 (widget explorations), T63.3 (drillin/history proposal), T63.4 (operator IA proposal).
- **GATE: David selects/reacts** (AskUserQuestion on T63.2; review of T63.3/T63.4).
- **Wave E63-B:** T63.5 (plan the implementation) -> the tasks it produces.

## Risks

| id | risk | p | impact | mitigation |
|---|---|---|---|---|
| R-E63-1 | Design explorations are built as production LiveView changes and churn mission_control_live.ex while E60's T60.3/T60.4 also edit it. | Med | Med | T63.2/T63.3/T63.4 are explicitly mock-only (static HTML); production edits happen only in Wave B tasks, which claim the file via the lane system. |
| R-E63-2 | The ETA question gets answered with a fabricated estimate that operators learn to distrust (the same trust failure as the stale attention queue). | Med | High | ADR-0046 honest-unknown is a hard rule in T63.4's acc: rate and progress only, never a promised date; label copy must say what the number is. |
| R-E63-3 | E63 and E60 ship two overlapping attention/portfolio surfaces. | Med | Med | T63.4's acc requires explicit reconciliation prose with T60.3/T60.4; the board's lane table marks the overlap; lead gates both merge paths. |
| R-E63-4 | T63.1's view-level staleness filter masks the registry bug so T60.2 loses urgency and ghosts persist underneath. | Low | Med | T63.1 is labeled defense-in-depth, not the fix; T60.2 stays open and claimable; the board tracks both. |

## Notes

- Fixture goal for T63.3 mocks: `runtime-gherkin-provider-adr-0071-loader`
  (the exact views David reported).
- The live dashboard log already showed the reaper reaping 1 zombie run at
  startup (2026-07-17 21:1xZ) -- corroborates the ghost-row mechanism behind
  concern 1.
