# E55 -- Teamwork is first-class: installed delivery, a bounded digest, and a board (P1, ADR-0071/0072/0073)

Back-link: [docs/plan.md](../plan.md) (master index). Sibling epics: [plans/](.)

fidelity: executable

E51 shipped the bus and it works: a `kazi daemon` carrying a dozen live
sessions across two machines and three projects, with cross-machine presence,
facts, and directed messages all round-tripping on the released binary. Adoption
still failed, and a 2026-07-16 observation pass found three structural causes --
none of which is agent discipline.

**Delivery was never built.** ADR-0067 point 6 shipped the turn-boundary hook as
a documented recipe. Its observed install rate is zero: no hook on either
machine, with the daemon up 16h. So the operator is the delivery mechanism, and
the reported symptom ("I am having to remind the agents over and over") is the
design working as specified. Two enhancement passes on the skill's bus section
changed no behaviour, because a recipe cannot fire itself.

**The digest protects the wrong reader.** `Kazi.Bus.Digest` is reached only from
the TTY branch of `emit/3` (`cli.ex:3951/3974/4010`), so `--json` and every
`kazi_bus_*` MCP tool -- the only paths agents and the documented hook use --
return the full transcript. A single `bus peek --json` on the live bus returns
the whole backlog verbatim, including two ~10-20 KB filler messages. E51's
R-E51-4 named this exactly; all three of its mitigations were raised (the 1 KiB
cap, lifted to 64 KiB for a real document-carrying need), deferred (T51.4), or
blocked on the deferred one (T51.6).

**Teams need state, not a stream.** Two project teams independently abandoned
the bus for hand-rolled append-only markdown blackboards ("coordination has
MOVED to the file blackboard ... re-read at every turn boundary") and did not
return after the blocking bug was fixed. A stream answers "what changed since I
looked"; a team needs "what is true now". `fact`'s last-value-per-subject
retention has been a board since day one with no verb to render it -- while the
fallback files are machine-local, so leaving the bus cost those teams the one
property it uniquely has. The duplicate work that follows is a lock-VISIBILITY
failure: claims live in `refs/claims/*` that the bus never hears about, so a
lead hand-maintains ownership in prose and a session declared closed picks up
its replacement's stream.

ADR-0071 (delivery is installed), ADR-0072 (the digest bounds context at render,
keeping the document-carrying limits), and ADR-0073 (the board projects roster +
ownership + current facts) decide the fixes. This epic builds them.

Acceptance: on the RELEASED binary, across two machines, a session started in a
project with hooks installed learns the team's state without a human telling it
to -- it registers, sees the board (roster, claims, current facts) at session
start, and receives bounded digests at turn boundaries, costing nothing when the
bus is quiet. `bus read --json` and every MCP bus tool return a digest bounded to
N lines regardless of backlog depth or message size, with `--full` escaping and
`bus get <id>` pulling a document deliberately. `kazi bus board` renders current
state cursor-free. A claim taken via `/claim` is visible on the board, and a
claim still succeeds identically with the daemon down. Sessions are addressable
by name. The dashboard shows the live roster instead of "No instances present".
Token cost is measured and recorded honestly in `docs/devlog.md`.

Supersedes within E51: **T51.4** (server-side digest deepening) is absorbed --
its brief predates the discovery that `--json` bypasses the digest entirely, so
as written it would deepen a digest that never reaches an agent; it lands as
T55.1 (the inversion + the render bound) plus T55.7 (the daemon move, its
original intent). **T51.6** (two-session live dogfood) is absorbed by T55.10, a
superset. **T51.5** stays in E51: it keeps run-lifecycle mirroring, and its
presence-rail half consumes T55.3's source wiring -- see R-E55-5, and do not
implement presence rendering twice.

Not in scope (owned elsewhere): `bus watch`'s backlog bug is **T54.9** (#1097) --
E55 must not touch `watch/1`. Per-task goal-files are authored by `kazi plan`
when each task starts.

## Wave A -- delivery + the bound (parallel)

- [ ] T55.1 The digest protects every machine-readable path (ADR-0072 d1/d2/d6): `--json` and every `kazi_bus_*` MCP tool return the digest by default, exactly as the TTY already does -- today `emit/3`'s JSON branch emits `%{"messages" => messages}` verbatim while `print_read_digest/1` sits in the fallback fn (`cli.ex:3951/3974/4010`, `mcp/server.ex`). Add `--full` (CLI) / `full: true` (MCP) as the documented escape. Bodies over a render threshold collapse to a one-line stub carrying id/kind/topic/provenance/size instead of rendering verbatim (the 64 KiB text / 128 KiB stream / 30-day limits STAY -- ADR-0072 keeps the document-carrying capability and bounds the render). Digest logic stays in `Kazi.Bus.Digest` (pure) this task; T55.7 moves it into the daemon. Two prerequisites this task owns: (a) the message id -- the JetStream stream sequence is already tracked internally for deduplication and STRIPPED before return (`bus.ex:271-275`, `Map.delete(&1, :stream_seq)`); stop stripping it and carry it on every digest line and stub as the public id T55.6 dereferences; (b) the envelope -- the digest JSON joins the ADR-0023 versioned contract (`schema_version`, shape in `kazi schema`), which today's bare `{ok, messages}` never honoured. Update `docs/session-bus.md` + `bus read|peek --help` + the MCP tool descriptions + the embedded skill's bus section (`install_skill.ex:622` -- the read/peek semantics it teaches change) (ADR-0034). Owner: pool  Est: 3h  kind: agent  verifies: [UC-068]  deps: []  locks: [R-cli-ex, R-session-bus-doc]  acc: ExUnit -- 200 posted messages across 3 kinds yield a `--json` digest bounded to <=40 lines with exact counts; a 60 KiB body renders as a stub carrying its stream-seq id, never verbatim; a directed message and a `sev: interrupt` appear verbatim (subject to the stub rule); `--full` returns every message unabridged; the digest JSON carries `schema_version`; the TTY path is unchanged; teach-coherence + doc-freshness green.
- [ ] T55.2 `kazi install-hooks` -- the opt-in installer (ADR-0071 d1/d3/d6): new `Kazi.Teach.InstallHooks`, sibling of `Kazi.Teach.InstallSkill` under the same contract -- explicit operator invocation only, a normal run NEVER writes harness config, `--dir` targets a tmp dir so tests never touch a real install. Writes a MARKED, idempotent block into the harness settings, merging and preserving every key it does not own; re-running is a no-op; `--uninstall` removes exactly what was added. The registered command is **`kazi bus hook <event>`** -- a kazi subcommand, not a script file (in-binary, unit-testable, upgrades with the binary; the settings block stays one line per event). Binding rule (ADR-0071 d2): ONLY events whose stdout reaches the session's context -- for the Claude Code profile that is `SessionStart` and `UserPromptSubmit`; a `Stop` hook's output never reaches the next turn, so binding the digest there is delivery to nowhere. Default install target is the USER-level settings (the hook no-ops anywhere without a daemon, so one install covers every project); `--project` writes the project's LOCAL (uncommitted) settings file; the installer NEVER writes a committed project file (public repo -- ADR-0034). Harness-agnostic by profile (ADR-0016); Claude Code is the only profile shipped. The hook payload itself is T55.9 -- this task lands the installer and the `kazi bus hook <event>` skeleton (always exits 0 silently, bounded wall-clock). Docs: README one-liner, `docs/session-bus.md`, AGENTS.md (the reverse doc-coherence guard REQUIRES new apply-adjacent surfaces be named there -- issue #973 landmine), `kazi help` (ADR-0034). Owner: pool  Est: 4h  kind: agent  verifies: [UC-068]  deps: []  locks: [R-cli-ex]  acc: ExUnit against a tmp dir -- install into ABSENT settings creates a valid file; install into settings holding an operator's own unrelated hooks + keys preserves every one of them byte-identically; install twice is a no-op (idempotent); `--uninstall` restores the pre-install bytes exactly; a malformed existing settings file fails with one clear line and writes NOTHING; the stub hook exits 0 silently with no daemon.
- [ ] T55.3 The dashboard renders the live bus, not an empty rail (ADR-0073 d4): `/leases` renders "Presence -- No instances present" while a dozen sessions are live, because `lease_map_live.ex:57` defaults `:lease_map_source` to `CoordinationSource.Native`, whose own moduledoc states presence is always empty (a native run announces none) -- bus presence lives in the daemon KV and is reachable only through `CoordinationSource.Transport`, which nothing configures. Default to the transport-backed source when a daemon is up, falling back to Native when it is not (a native run renders exactly as today). Read-only projection contract unchanged (ADR-0011). Files are `lib/kazi_web/**` ONLY -- this task is deliberately disjoint from every other task in the epic and needs no locks. Coordinate with T51.5's rail half (R-E55-5). Owner: pool  Est: 3h  kind: agent  verifies: [UC-068]  deps: []  locks: []  acc: ExUnit/LiveView -- with a stubbed transport snapshot the presence rail renders roster rows (session, machine, last-seen); with no daemon the view falls back to Native and renders today's output unchanged (no crash, no 500); the source choice is observable, not hardcoded.

## Wave B -- the board + identity (parallel)

- [ ] T55.4 `kazi bus board` -- the current-state projection (ADR-0073 d1): new `Kazi.Bus.Board` rendering last-value `fact` per topic + the live roster (names, teams), from the last-value retention ADR-0067 point 3 already provisions (the claim-ownership section is T55.8's refs projection and lands there). Cursor-free and idempotent: it consumes NOTHING, so a session may read it every turn without draining what a `read` was counting on (contrast the `bus read` ack landmine). Bounded by T55.1's digest rules. This is what the T55.9 session-start hook injects and what retires the machine-local markdown blackboards. Mirror as MCP tool `kazi_bus_board` (ADR-0044/0067 convention: every bus verb has its MCP twin). Update `docs/session-bus.md` + help + the embedded skill's bus section (`install_skill.ex:622`) (ADR-0034). Owner: pool  Est: 4h  kind: agent  verifies: [UC-068]  deps: [T55.1]  blocked-by: [T55.1]  locks: [R-cli-ex, R-session-bus-doc]  acc: ExUnit (`:nats`) -- posting three facts on one topic renders ONE current line (the last value, not three); `board` twice returns identical output and a subsequent `read` still sees every pending message (nothing consumed); the roster reflects presence TTL; a 60 KiB fact renders as a stub (T55.1 rule); output is bounded regardless of topic count.
- [ ] T55.5 Stable, addressable session identity (ADR-0073 d3): `kazi bus name <nickname>` assigns a durable name carried on presence and rendered by `who`, `tell`, and the board; `tell` resolves a nickname, an `@team`, or a raw session id. Replace the `os-<pid>` fallback -- four such rows are live today, unaddressable and re-registering as a new "session" every process -- with an id stable across the session's processes, so a session cannot fragment into ghost rows. Extends the ADR-0067 point 2 resolution chain with a durable layer; does not replace it. Today sessions work around this by broadcasting "I am <name>" as free-text facts and `@`-mentioning inside message bodies, so directed messaging -- the fourth gap ADR-0067 set out to close -- is unusable for want of a name. Nickname resolution applies to the MCP twins too (`kazi_bus_tell` takes a nickname; add `kazi_bus_name`). Update `docs/session-bus.md` + help + the embedded skill's bus section (`install_skill.ex:622`) (ADR-0034). Owner: pool  Est: 3h  kind: agent  verifies: [UC-068]  deps: []  locks: [R-cli-ex, R-session-bus-doc]  acc: ExUnit (`:nats`) -- `bus name` then `bus who` shows the nickname; `bus tell <nickname>` reaches the same consumer as `bus tell <session-id>`; a session whose name detection fails gets a stable id twice in a row (no ghost row); an unknown nickname is a one-line error naming the roster, not a silent drop.
- [ ] T55.6 `kazi bus get <id>` -- deliberate pull for stubbed content (ADR-0072 d3): full text becomes addressable and fetched on purpose by a session that has decided it is worth the context, so T55.1's stubs stay reachable. The id is the JetStream stream sequence T55.1 surfaces (direct stream get by seq -- no consumer, so `get` never disturbs anyone's cursor); `--full` prints the body unabridged. Reading a document becomes a visible choice rather than an ambush inside a routine check. Mirror as MCP tool `kazi_bus_get`. Update `docs/session-bus.md` + help + the embedded skill's bus section (`install_skill.ex:622`) (ADR-0034). Owner: pool  Est: 2h  kind: agent  verifies: [UC-068]  deps: [T55.1]  blocked-by: [T55.1]  locks: [R-cli-ex, R-session-bus-doc]  acc: ExUnit (`:nats`) -- a 60 KiB post renders as a stub in the digest, and `bus get <that id>` returns the body byte-identical; an unknown/expired id is a one-line error; `get` consumes nothing (a later `read` still delivers the message).

## Wave C -- server-side, claims, and the payload (parallel)

- [ ] T55.7 Digest assembly moves into the daemon (ADR-0072 d5; absorbs T51.4's original intent): the client sends `read` over the control socket; the daemon pulls the consumer, aggregates last-value facts per topic, collapses repeated presence/intent, and enforces verbatim-only-for-directed-or-interrupt before the bytes ever reach a client. This is ADR-0067 point 5's own load-bearing argument -- "only a server can aggregate before the tokens are spent" -- and it is what keeps the CLI, the MCP tools, and the T55.9 hook from re-implementing the bound three times. Carries T51.4's `bus read --since <cursor>` debugging escape. Owner: pool  Est: 4h  kind: agent  verifies: [UC-068]  deps: [T55.1]  blocked-by: [T55.1]  locks: []  acc: ExUnit (`:nats`) -- the daemon returns a bounded digest for a 200-message backlog and the CLI renders it without re-aggregating; CLI, MCP, and hook paths produce IDENTICAL digests for the same backlog; `--since <cursor>` replays from a point; with the daemon down every bus surface still reports the clean one-line no-daemon error (ADR-0067 point 1).
- [ ] T55.8 Claim visibility -- the board reads `refs/claims/*` at source (ADR-0073 d2): the board's ownership section projects `git ls-remote origin 'refs/claims/*'` directly. Claims are ALREADY cross-machine (the claim primitive pushes every claim to origin) and ALREADY self-describing (the claim commit message carries owner, host, timestamp) -- so there is no copy to drift, no staleness class, and no daemon anywhere in the claim path; claiming with the daemon down is unaffected by construction, not by discipline. Render owner + age per claim; a short network timeout, and when the remote is unreachable the section degrades to ONE honest "claims unavailable (remote unreachable)" line -- never a possibly-stale table, and local refs are never trusted as current. kazi does NOT modify the operator's claim tooling (it lives outside this repo's delivery; a correctness dependency on editing it would repeat ADR-0067 point 6's failure) -- but `docs/session-bus.md` documents an OPTIONAL best-effort one-liner (`kazi bus post fact "claimed <task>" --topic claims`) a claim wrapper MAY add so turn-boundary digests announce claim events; the board never renders ownership from it. Owner: pool  Est: 3h  kind: agent  verifies: [UC-068]  deps: [T55.4]  blocked-by: [T55.4]  locks: [R-session-bus-doc]  acc: ExUnit -- against a fixture remote holding two claim refs the board renders both with owner + age parsed from the claim commits; taking/releasing a claim appears/vanishes on the next board render with NO daemon involvement (pinned with the daemon down); an unreachable remote yields the one-line degradation within the timeout (no crash, no stale render); the optional digest one-liner is documented.
- [ ] T55.9 The hook payload -- what actually reaches a session (ADR-0071 d2/d4/d5): fill the `kazi bus hook <event>` skeleton from T55.2. `hook session-start` (Claude Code: `SessionStart`) registers presence + joins the project-scope team + injects the board (T55.4); `hook turn` (Claude Code: `UserPromptSubmit` -- the event whose stdout reaches the turn's context) injects the digest ONLY when there is traffic and stays silent otherwise -- this is the property that ends the token complaint, since ambient awareness costs nothing while the bus is quiet and needs no blocked `watch` and no poll loop. Both no-op silently with the daemon down (ADR-0067 point 1), and both carry a HARD wall-clock bound: answer within the budget (~2s) or exit 0 silently, because a slow or hung daemon must never tax every turn of every session on the machine. The injected block is provenance-stamped and framed as untrusted, advisory external input, never a command channel (ADR-0071 point 5 / ADR-0067 point 7) -- injection is the moment that contract matters, because a hook folds agent-authored text into another agent's context. Owner: pool  Est: 4h  kind: agent  verifies: [UC-068]  deps: [T55.1, T55.2, T55.4]  blocked-by: [T55.1, T55.2, T55.4]  locks: []  acc: ExUnit + a real harness session -- a quiet bus injects NOTHING (zero bytes, verified, not merely "small"); a bus with traffic injects a bounded digest; session-start injects the board and the session appears in `bus who` with its team; daemon down injects nothing and the session proceeds identically; a HUNG daemon (stalled-socket fixture) exits 0 silently within the bound; the injected block carries provenance and the advisory framing verbatim.

## Wave D -- the honest close

- [ ] T55.10 LIVE dogfood -- a team that nobody reminds (absorbs T51.6): on the RELEASED binary across two machines, run concurrent sessions in >=2 projects through real work with hooks installed. Measure and record in `docs/devlog.md`: the token cost of a turn-boundary digest (vs the pre-E55 raw-transcript baseline measured the same way), whether a starting session reached the board without a human prompt, whether two sessions ever duplicated claimed work, and every friction point as a filed issue. The bar is the reported symptom, honestly assessed: the operator did not have to tell a session to check the bus. Report what was actually observed, including anything that fell short. Owner: pool  Est: 3h  kind: any  verifies: [UC-068]  deps: [T55.3, T55.5, T55.6, T55.7, T55.8, T55.9]  blocked-by: [T55.3, T55.5, T55.6, T55.7, T55.8, T55.9]  locks: []  acc: observed evidence in `docs/devlog.md` on the RELEASED binary, cross-machine, with the token-cost delta stated as a measured number and an explicit verdict on the no-reminder bar.

## Waves

- **Wave E55-A (3 parallel):** T55.1 (the bound), T55.2 (the installer), T55.3 (the dashboard). The two highest-leverage items plus the one that touches no shared file.
- **Wave E55-B (3 parallel):** T55.4 (board), T55.5 (identity), T55.6 (`bus get`). T55.5 has no deps and may start in Wave A if a session is free and R-cli-ex is uncontended.
- **Wave E55-C (3 parallel):** T55.7 (daemon digest), T55.8 (claims), T55.9 (hook payload).
- **Wave E55-D:** T55.10 closes the epic post-release.

**Ordering constraint that is not a dep edge:** do NOT enable hook delivery to
real sessions before T55.1 has landed. The installer (T55.2) and the bound
(T55.1) are independently correct and genuinely parallel -- different files
entirely -- but pushing today's raw-transcript read into every session at every
turn boundary would automate R-E51-4 rather than fix it. T55.9 carries the dep
edge; Wave A carries the discipline.

**Shared-file locks.** `lib/kazi/cli.ex` (R-cli-ex) and `docs/session-bus.md`
(R-session-bus-doc) are touched by several tasks, and the verb tasks
(T55.1/4/5/6) also each touch the embedded skill's bus section
(`install_skill.ex:622`) and the MCP server's tool table -- treat those two
under R-cli-ex as well rather than minting more locks. Each touch is small and
additive -- one `@bus_verbs` entry, one `execute_bus/3` clause, one MCP tool
row, one help line -- so take the lock for the wiring commit only and release
it immediately; do not hold it across implementation. T55.3, T55.7, and T55.9
need no locks; T55.8 takes R-session-bus-doc for its one doc addition.

## Risks

| id | risk | p | impact | mitigation |
|---|---|---|---|---|
| R-E55-1 | `install-hooks` clobbers or corrupts an operator's existing harness settings -- kazi's first WRITE surface in harness config. | Med | High | marked idempotent block, merge-never-clobber, `--uninstall` restores pre-install bytes, `--dir` tmp in tests (never a real install), malformed-input writes nothing; T55.2's acc pins a settings file holding an operator's own unrelated hooks. |
| R-E55-2 | Hook delivery ships before the render bound and floods every session every turn -- R-E51-4, now automated. | Med | High | T55.9 deps on T55.1; the Wave-A ordering constraint above is explicit; T55.10 measures the delta against a baseline rather than asserting it. |
| R-E55-3 | Every turn pays the hook's wall-clock; a slow or HUNG daemon taxes every session on the machine -- a failure mode worse than any missed digest. | Med | Med | hard internal timeout in `kazi bus hook` (~2s), exit 0 silent on expiry; daemon-down is a fast no-op (connect refused); T55.9's acc pins the hung case with a stalled-socket fixture, not just the down case. |
| R-E55-4 | The board's ownership section needs the repo remote; offline or flaky network makes it render stale or hang the board. | Med | Low | claims are read AT SOURCE (`ls-remote`) with a short timeout; unreachable degrades to one honest "claims unavailable" line, never a stale table; local refs are never trusted as current (T55.8 acc). The drift class this row originally tracked (auto-posted facts as a second copy of ownership truth) was designed out -- ADR-0073 d2 rejects it as drift-prone AND unshippable. |
| R-E55-5 | Dashboard presence gets built twice -- T55.3 (source default) and T51.5 (starmap rail) -- the exact duplicate work this epic exists to prevent. | Med | Low | named here and in both task briefs: T55.3 owns the CoordinationSource default, T51.5 owns the rail and CONSUMES it; whichever lands second narrows to what is left. |
| R-E55-6 | Hook injection becomes a prompt-injection vector into every session on the machine. | Med | Med | ADR-0071 point 5 / ADR-0067 point 7: the injected block is provenance-stamped and framed as untrusted advisory input, never a command channel; T55.9's acc pins the framing text; the 1-way advisory contract is stated in the surfaces' own help. |
| R-E55-7 | `--json` returning a digest breaks a caller parsing `.messages[]` today. | High | Low | deliberate and ADR-0072-sanctioned: the current shape IS the defect; `--full` restores it; the change is named in the CHANGELOG and `docs/session-bus.md`, and the only in-repo consumers are kazi's own surfaces. |
