# E70 -- Triage 2026-08-30: full open-backlog resolution, second pass -- false-green/false-stuck --parallel verdicts, disk-pressure worktree placement, predicate-authoring hazards, scope-boundary enforcement (#1690-#1708)

Back-link: [docs/plan.md](../plan.md) (master index). Sibling epics: [plans/](.)

fidelity: executable

The 2026-08-30 full open-issue triage pass. **E69 (2026-08-08, 14 tasks, 3
waves) is still 0/14 -- fully unstarted, 22 days after being planned; none
of its 14 issues have closed.** This epic does NOT re-decompose E69; it
covers the 16 issues that opened SINCE that triage (#1690-#1705) and were
never given a plan home. Read together, E69 + E70 are the entire open
backlog (30 real issues; #382/#372 continue to ride T25.10 in E25,
unchanged). `/apply --pool` should treat E69's Wave A and E70's Wave A as
one combined P0 front -- both are "kazi's own reported verdict lied,"
independently discovered 22 days apart, and neither blocks the other.

Three issues in this batch (#1692, #1699, #1702) posed genuine
product-direction forks rather than obvious bugs, and were decided by the
operator before this epic was materialized:

- **#1692** (claim `transfer` verb) -- **REDIRECTED, no kazi-repo task.**
  Traced at triage time: `lib/kazi/bus/claims.ex`'s own moduledoc states
  claim/release/transfer logic lives OUTSIDE this repo, in the global
  Claude Code skill (`~/.claude/skills/claim/scripts/claim.sh`), per
  ADR-0067 point 6 -- an already-ratified boundary this triage does not
  reverse. T70.11 closes #1692 on kazi-org/kazi with that disposition;
  the actual `transfer` verb (if built) is a direct edit to the global
  skill script, tracked nowhere in this repo's WBS.
- **#1702** (auto roadmap-note PR) -- **REMOVE ENTIRELY.** Same
  scope-boundary principle as #1695/#1704 (T70.9): kazi's own automation
  should not write process/narrative files after convergence, full stop.
  T70.10 removes the auto-generated roadmap-note PR rather than gating it
  behind an opt-in flag.
- **#1699** (launcher-liveness reaping a deliberately-backgrounded run)
  -- **FIX THE CHECK.** T70.4 makes `Kazi.Runtime.ParentMonitor` distinguish
  a process reparented to init by deliberate `nohup`/`disown`/`setsid`
  backgrounding from an actually-dead launcher, rather than only
  documenting the constraint.

Code anchors verified on origin/main at triage time (2026-08-30):
`Kazi.ReadModel.HeartbeatTicker.handle_info/2` (lib/kazi/read_model/
heartbeat_ticker.ex:41-59, three case clauses -- `{:ok, _}`,
`{:error, :not_found}`, `{:error, :read_model_unavailable}` -- no clause for
`{:error, %Ecto.Changeset{}}`), `Kazi.Loop.terminate_workspace_missing/2` +
`git_worktree_gone?/1` (lib/kazi/loop.ex:3586-3615, the `:workspace`
synthetic-predicate stuck path), `Kazi.Scheduler.Worktree.default_base_dir/0`
(lib/kazi/scheduler/worktree.ex:212-215, unconditional `System.tmp_dir!()`),
`Kazi.Enforcement.Isolation` (lib/kazi/enforcement/isolation.ex:83,148, same
unconditional tmp-dir pattern), `Kazi.Runtime.ParentMonitor` (lib/kazi/
runtime/parent_monitor.ex:155, the "launcher process ... is gone" reap),
`Kazi.Providers.CustomScript.apply_verdict("match_count", ...)`
(lib/kazi/providers/custom_script.ex:300-361, the verdict evaluator whose
`--parallel`-path bypass #1694 reports), `Kazi.Bus.Claims` moduledoc
(lib/kazi/bus/claims.ex:1-29, the ADR-0067 pt 6 ownership boundary cited for
#1692's redirect). Next ADR number at plan time: **0085** (this epic's own
T70.9 decision, filed as `docs/adr/0085-scope-goal-file-forbidden-paths-
commands.md`, Accepted 2026-08-30) -- **note the collision risk**: E69's
T69.12 also reserved "~0084" for its own #1642 decision, and NEITHER epic
has executed yet. Whichever of T69.12 / T70.9 lands first takes 0084; the
other MUST re-verify via `ls docs/adr/` and take the next free number at
execution time, not assume its plan-time reservation still holds.

**Two more issues (#1707, #1708) opened during this same triage window** (a
disk-pressure incident on a shared machine delayed this epic's write by
roughly an hour; both were caught on re-check before the plan write, not
missed): #1708 is a HARD APPLICATION CRASH (the whole OTP node terminates,
Erlang crash dump written to disk) when `apply --parallel` runs without
`--workspace` against a goal whose own scope also has no
workspace/repo -- `Kazi.Scheduler.Partitioner.partition/3`'s guard clause
(`is_binary(workspace)`) has no fallback, so a `nil` workspace is an
uncaught `FunctionClauseError` that kills the application instead of a
clean validation error. #1707 is two related problems in one goal: (a)
`kazi integrate`'s generated PR title/body lists ONLY the predicates that
converged (`passed_predicates/1`, integrate.ex:480-484, feeds BOTH the PR
title at :465 and the body at :380/:460) -- a caller reading the PR alone
has zero indication that other, non-converged predicates existed, which is
the exact same "kazi's own reported surface lies by omission" class as
Wave A's #1694/#1696/#1703, just on the PR artifact instead of the JSON
status; (b) `swift_test`/xcodebuild capability predicates cannot execute at
all inside `--parallel`'s ephemeral worktree (no `.xcresult` ever
produced, `:error` not `:fail`) -- a distinct, still-open environment gap
from the already-fixed #1406 vacuous-green class. Folded in as T70.14
(#1708 crash, Wave A), T70.15 (#1707a PR omission, Wave A), T70.16 (#1707b
swift_test/xcodebuild under --parallel, Wave B).

Acceptance: every issue above is closed citing a merged PR (or an explicit
recorded disposition, per #1692/#1702/#1699 above); the #1694/#1696/#1703/
#1707a/#1708 fixes are each proven by a red-to-green (or false-to-honest,
or crash-to-clean-error) replay of the exact repro from the issue, not a
synthetic stand-in; no fix weakens an existing assertion; docs land with
the code (ADR-0034); fixes reaching a release are verified on the RELEASED
binary where the symptom was release-binary-specific (#1694, #1696, #1698,
#1699, #1703, #1707, #1708 were all observed on 1.273.7/1.275.0/1.275.1
burrito builds, not `mix test`).

## Tasks

- [ ] T70.1 Fix #1694 -- `match_count` verdict predicates report `pass` on
  zero matches under `--parallel` (P0, defeats #1690's own recommended
  mitigation): a 5-predicate goal (4 `match_count` capability predicates +
  1 `exit_zero` guard) converged with all 5 `pass` and the landing partition
  branch at ZERO commits / empty diff against base -- confirmed by manually
  re-running the exact predicate command in the exact worktree (`go test
  -run '<name>' -v` -> "no tests to run", zero PASS lines, process exits 0).
  `apply_verdict("match_count", ...)` (custom_script.ex:325-361) correctly
  implements match_regex/pass_when when called directly; find where the
  `--parallel`/scheduler path diverges -- either it evaluates verdicts
  against a cached/stale t0 observation instead of re-observing post-dispatch,
  or it falls back to the wrapped command's raw exit code somewhere in the
  scheduler's convergence check rather than routing through
  `apply_verdict/4`. Fix so `--parallel` and `--check` agree on the verdict
  for the identical predicate + identical (non-)diff.  Owner: pool  Est: 5h
  kind: agent  lane: agent  verifies: [infrastructure]  deps: []
  acc: [a fixture goal with a match_count capability predicate whose target
  test does not exist yet reports the SAME red verdict under --check and
  under --parallel when no commits land (pinned); once a commit adds the
  matching test, both paths report the same green verdict (pinned); red on
  pre-fix main reproducing the exact #1694 shape (zero diff, pass verdict)]

- [ ] T70.2 Fix #1703 -- `HeartbeatTicker` `CaseClauseError` on an
  `Ecto.Changeset` `:timeout` error crashes the ticker mid-run, and the run's
  TERMINAL RESULT still reports `"collective":"converged"` against a branch
  with zero diff and zero commits (worse than the related, already-tolerated
  `{:error, :read_model_unavailable}` case in #1511 -- that one only logged
  noise, this one coincides with a false top-line success). Add a case clause
  in `handle_info/2` (heartbeat_ticker.ex:41) for `{:error, %Ecto.Changeset{}}`
  (and any other write-failure shape `RunRegistry.heartbeat/1` can return)
  that degrades the SAME way `:read_model_unavailable` already does (log
  once, keep ticking, never crash the GenServer) -- but ALSO: a ticker crash
  must never be interpretable anywhere downstream as convergence evidence.
  Audit whether the crash is merely coincidental noise (the real bug is
  purely in the convergence-reporting path #1696/#1694 also hit) or whether
  the ticker crash itself starves a supervision link the terminal-result
  computation depends on. Fix whichever is root cause; a follow-up-only fix
  that suppresses the crash log without addressing why an empty-diff branch
  reported converged is NOT sufficient to close this issue.  Owner: pool
  Est: 4h  kind: agent  lane: agent  verifies: [infrastructure]  deps: []
  acc: [handle_info/2 has a case clause for {:error, %Ecto.Changeset{}}
  timeout that does not crash the GenServer (pinned, ExUnit constructs the
  exact changeset shape from the issue); a fixture run whose target branch
  has zero diff from base never reports collective: converged regardless of
  whether the ticker crashes concurrently (pinned); red on pre-fix main]

- [ ] T70.3 Fix #1696 -- `apply --parallel` reports the goal `stuck` against
  a synthetic `:workspace` predicate after the goal's REAL predicates already
  converged and committed to the target branch. Root cause per the captured
  log: the scheduler's own ephemeral partition worktree under
  `kazi-worktrees/p-<hash>` was removed (by the reaper, or never fully
  materialized on a later re-check) WHILE the run's target-branch commits
  had already landed; `terminate_workspace_missing/2` +
  `git_worktree_gone?/1` (loop.ex:3586-3615) then fires unconditionally on
  the missing scratch dir, producing a `:workspace_missing` stuck cause that
  looks identical to a goal that genuinely never converged. Fix: before
  terminating `:workspace_missing`, check whether the target branch (not the
  ephemeral scheduler worktree) already carries commits satisfying the
  goal's predicate set -- if so, report `converged` (the workspace loss is
  irrelevant bookkeeping noise after the fact), never `stuck`. If that check
  cannot be made cheaply/safely, at minimum make `:workspace` distinguishable
  from the caller's own authored predicates in the terminal JSON (a distinct
  `cause` field, not folded into the same failing-predicate-id namespace) so
  an orchestrator does not misread "kazi's bookkeeping glitched" as "the goal
  needs re-dispatching."  Owner: pool  Est: 5h  kind: agent  lane: agent
  verifies: [infrastructure]  deps: []
  acc: [a fixture that lands real convergent commits on the target branch
  and then has its ephemeral partition worktree removed out from under the
  scheduler reports collective: converged, not stuck (pinned); if full
  reconciliation is infeasible, the terminal JSON's :workspace-caused stuck
  is distinguishable from an authored-predicate-caused stuck by a named
  field (pinned); red on pre-fix main reproducing the exact log shape]

- [ ] T70.4 Fix #1699 (DECIDED 2026-08-30: fix the check) -- `apply
  --parallel`, launched via `nohup ... & disown` (or `setsid`) so it
  deliberately reparents off its invoking shell, gets reaped within ~26s as
  "launcher process 1 is gone" (parent_monitor.ex:155) before any dispatch
  happens -- zero JSONL progress, no goal-file/predicate problem, silent-ish
  (exits with a short warning easy to miss when backgrounded/logged). The
  reporter confirmed this specifically follows from deliberate
  backgrounding (reparent-to-init reads as "gone"), not host load -- a
  supervised invocation with a live, non-orphaning parent does not hit this
  halt. Fix `Kazi.Runtime.ParentMonitor` to distinguish "reparented to pid 1
  because the operator intentionally backgrounded us" from "the launcher
  actually died" (e.g. checking whether the new parent is genuinely pid 1 in
  a way consistent with intentional detachment, vs. the launcher process no
  longer existing at all) so `nohup`/`disown`/`setsid` invocations survive.
  If a reliable distinction is not possible, downgrade to documenting the
  constraint explicitly in `kazi apply --help` and the orchestrator recipe
  (not silently reaping with an easy-to-miss warning).  Owner: pool  Est: 3h
  kind: agent  verifies: [infrastructure]  deps: []
  acc: [a kazi apply process backgrounded via nohup+disown (or setsid) in a
  fixture completes at least one dispatch instead of being reaped within
  30s (pinned, red on pre-fix main); a genuinely-dead launcher (parent
  process actually killed) still triggers the reap (pinned, no regression)]

- [ ] T70.5 Fix #1698 -- scheduler partition worktrees ignore `--workspace`,
  always land under `$TMPDIR` (P0: directly contributed to a 2026-08-29/30
  fleet-wide disk-pressure incident -- internal disk under 500MB free with
  ~88 concurrent sessions, several running `--parallel` with `--workspace`
  deliberately pointed at an offload volume specifically to avoid this).
  Confirmed root cause (issue comment, verified in source):
  `Kazi.Scheduler.Worktree.default_base_dir/0` (worktree.ex:212-215) and
  `Kazi.Enforcement.Isolation` (isolation.ex:83,148) both hardcode
  `Path.join(System.tmp_dir!(), ...)` with no override. Fix: derive the
  scheduler's own partition-worktree base dir from the volume `--workspace`
  resolves to by default (a sibling directory of the resolved `--workspace`
  path, e.g. `<workspace-parent>/.kazi-worktrees/`), not unconditionally
  `$TMPDIR`; also add an explicit `--base-dir`/config-key override for
  callers who want a third location. Apply the same derivation to
  `Kazi.Enforcement.Isolation`'s enforce-checkout tmp dir.  Owner: pool
  Est: 4h  kind: agent  verifies: [infrastructure]  deps: []
  acc: [kazi apply --workspace <path-on-volume-A> --parallel --explain (or
  equivalent inspection) shows scheduler partition worktrees resolving
  under volume A, not $TMPDIR, by default (pinned); an explicit
  --base-dir/config override is honored when given (pinned); enforcement
  isolation checkouts follow the same derivation (pinned)]

- [ ] T70.6 Fix #1697 -- ephemeral converge/enforce checkouts under
  `$TMPDIR` are never reaped (compounds T70.5; distinct defect -- even with
  perfect placement, a live run's partitions correctly sit on disk for its
  duration, but FINISHED runs' checkouts (`kazi-worktrees/p-*`,
  `kazi-enforce-<pid>`) were observed 21h stale with no live process holding
  them, 2.1GB + 722MB). Fix: (a) converge/enforce remove their scratch
  checkout on exit, INCLUDING on interrupt/kill (a trap/after-clause, not
  only the happy path); (b) the daemon reaps orphaned `kazi-worktrees/p-*`
  and `kazi-enforce-*` directories older than a TTL at startup and on a
  schedule (mirrors the existing ghost-row/run reaping pattern); (c) a `kazi
  gc` (or equivalent) verb reaps orphans on demand with a dry-run mode; (d)
  an opt-out flag preserves checkouts for debugging.  Owner: pool  Est: 4h
  kind: agent  verifies: [infrastructure]  deps: []
  acc: [a converge/enforce run killed mid-flight (SIGTERM) leaves no
  kazi-worktrees/p-* or kazi-enforce-* checkout behind (pinned); a fixture
  seeded with orphaned checkouts older than the TTL is reaped by the
  daemon's sweep and by kazi gc --dry-run/--apply (pinned)]

- [ ] T70.7 Predicate-authoring lint hazards -- three related unsatisfiable-
  or-false-passing shell/regex shapes, same family as the already-open
  #1690 (`kazi lint` should already flag `go test -run <name>` + `exit_zero`
  as a zero-match false-green hazard; still open, folded in here): (a)
  #1693 -- a `grep -c <pattern> | ...` (or bare `grep -c`) assignment under
  an implicit/explicit `set -e`, unguarded by `|| true`/`|| echo 0`, aborts
  the script at the ASSIGNMENT LINE exactly when the count is zero (grep's
  exit code reflects match/no-match) -- exactly the success case for a
  "this must NOT appear" guard predicate, producing a false STUCK on
  correct work; (b) #1701 -- a `match_regex`/grep pattern anchored
  `^--- PASS: <name>$` (trailing `$` immediately after the test name, no
  `.*`/duration placeholder) can never match real `go test -v` output,
  which always appends ` (0.00s)`; this is a REGRESSION of #1690's own
  already-correct recommended shape (`^--- PASS: TestName` with no trailing
  `$`), so whatever generates these predicates is inconsistent. Implement:
  `kazi lint` flags (1) `custom_script` predicates whose `cmd`/script counts
  matches via `grep -c`/plain `grep`-as-counter/`wc -l` piped from such a
  grep, assigned under `set -e` without an `|| true`/`|| echo 0` guard; (2)
  `go test -run <name>` + `exit_zero` verdict shape (#1690, if not already
  covered); (3) a `match_regex` ending in a literal `$` immediately after a
  test-name capture with no duration/whitespace placeholder before it, when
  paired with a `go test -v`-shaped command. Update `AUTHORING.md` alongside
  each with the correct pattern (mirrors #1690's own prescribed fix,
  `match_regex = "^--- PASS: TestName"`, `verdict = "match_count"`,
  `pass_when = ">= 1"`, no trailing `$`).  Owner: pool  Est: 4h  kind: agent
  verifies: [infrastructure]  deps: []
  acc: [kazi lint flags all three predicate shapes above against fixture
  goal-files reproducing #1690/#1693/#1701 exactly (pinned, each red on
  pre-fix main); a corrected predicate in each fixture shape passes lint
  clean (pinned); AUTHORING.md documents the correct pattern for each]

- [ ] T70.8 Fix #1700 -- a `vitest run -t "<name>"` predicate asserting the
  aggregate summary reads exactly `Tests  1 passed (1)` is unsatisfiable in
  any project with more than one spec file: vitest always collects every
  file matched by `test.include` and marks non-matching tests `skipped`
  rather than excluding them, so the real summary is `N passed | M skipped
  (total)` whenever M > 0, with no reporter/flag that drops the skipped
  segment (verified against vitest 2.1.9, default/verbose/basic reporters,
  `CI=true` or not). Whatever authoring path generates a `-t`-scoped vitest
  predicate must stop assuming a from-scratch/single-file project. Fix the
  goal-authoring prompt/template to assert on the PER-TEST result line
  (e.g. `grep -Eq '^\s*(pass|PASS|.)\s.*<test name>'` matching vitest's
  per-test marker) or scope the invocation to the specific spec file as a
  positional filter so skip-count only covers that file, and to match
  `Tests\s+1 passed\s+\|\s+\d+ skipped \(\d+\)` instead of the zero-skip
  `(1)` case when the aggregate form is used at all. Document the corrected
  pattern in AUTHORING.md next to the go-test equivalents from T70.7.
  Owner: pool  Est: 1.5h  kind: agent  verifies: [infrastructure]  deps: []
  acc: [AUTHORING.md documents a vitest -t predicate pattern that a fixture
  multi-spec-file project satisfies when the target test passes (pinned
  against the exact #1700 repro shape: 1 matching test, multiple spec
  files, real "N passed | M skipped" summary)]

- [ ] T70.9 ADR-0085 + implement `[scope]` goal-file block: `forbidden_paths`
  (a list of path globs the grind loop's own commit/landing tooling refuses
  to touch, enforced as an automatic guard predicate + a landing-time
  refusal -- not merely detected after the fact) and `forbidden_commands` /
  `no_integration` (declared negative-action contract; `no_integration`
  reuses the existing `[integration]` `none` mode as its enforcement
  primitive; `forbidden_commands` is best-effort dispatch-transcript
  scanning, documented explicitly as non-sandboxing). Closes BOTH #1695
  (grind loop committed a change to `docs/plan.md`/`docs/roadmap.md`
  despite a prose exclusion list in its dispatch brief) and #1704 (a grind
  model opened a PR and edited a docs file despite an orchestrating
  session's prose "do not open a PR" instruction it never saw -- per
  `AUTHORING.md`, the dispatch prompt is the ONLY channel the grind model
  reads). ADR first: docs/adr/0085-scope-goal-file-forbidden-paths-
  commands.md (already drafted and Accepted as part of this triage pass --
  verify the number is still free at execution time per the collision note
  above). Docs: goal-file schema reference + an AUTHORING.md section
  showing the exact `[scope]` shape that would have caught #1695/#1704.
  Owner: pool  Est: 5h  kind: agent  lane: agent  verifies: [infrastructure]
  deps: []
  acc: [a fixture goal with [scope] forbidden_paths = ["docs/plan.md",
  "docs/roadmap.md"] fails its guard predicate (and landing refuses to
  include the touched path) when a dispatched commit touches one of those
  paths, reproducing #1695 as a red-then-fixed pin; a fixture goal with
  [scope] no_integration = true never lands a PR even when the dispatched
  model attempts gh pr create, reproducing #1704's ask; both documented in
  the goal-file schema reference]

- [ ] T70.10 Fix #1702 (DECIDED 2026-08-30: remove entirely) -- kazi's
  post-convergence flow auto-opens a second, separate pull request (branch
  pattern `docs/<goal>-shipped-note`) appending a thin auto-generated
  `docs/roadmap.md` status note, independent of the actual code PR for the
  converged branch and independent of any caller-side coordination
  (including a project's own `/claim`-based lock over `docs/roadmap.md` --
  that lock protects a DIFFERENT edit path than this auto-PR uses, so two
  PRs can append near-duplicate content to the same file/anchor with no
  collision detection). Locate the note-generation step (grep the
  `-shipped-note` branch-name pattern and the post-convergence/landing
  pipeline around `lib/kazi/actions/integrate.ex` and `lib/kazi/scheduler/
  integration.ex` -- not pinned to an exact line at plan time) and remove
  it: convergence leaves `docs/roadmap.md` entirely to the caller, matching
  the T70.9 scope-boundary principle (kazi's own automation does not write
  process/narrative files after convergence). If any existing ADR
  documents this behavior as intentional, note the removal in that ADR's
  Consequences (do not silently contradict a live ADR).  Owner: pool
  Est: 2.5h  kind: agent  verifies: [infrastructure]  deps: []
  acc: [a converged goal's post-convergence flow opens exactly one PR (the
  code PR), never a second roadmap-note PR, in a fixture run (pinned, red
  on pre-fix main reproducing the two-PR shape); no dangling reference to
  the removed note-PR step remains in docs/help text]

- [ ] T70.11 Disposition #1692 (DECIDED 2026-08-30: redirect, no kazi-repo
  code task) -- close #1692 on kazi-org/kazi with a comment citing
  `lib/kazi/bus/claims.ex`'s own moduledoc (claim/release/transfer identity
  work "lives in the claim tooling OUTSIDE this repo's delivery, ADR-0067
  point 6") as the reason a `transfer <id> <expected-sha> <new-owner>` verb
  is not a kazi-org/kazi task; point at `~/.claude/skills/claim/scripts/
  claim.sh` as the actual location for the fix, and note it is not
  version-controlled inside this repo so no PR here can close it. This is a
  disposition-only task: no code change in kazi-org/kazi.  Owner: pool
  Est: 0.25h  kind: agent  verifies: [infrastructure]  deps: []
  acc: [#1692 carries a disposition comment citing lib/kazi/bus/claims.ex
  and ADR-0067 point 6, and is closed or explicitly relabeled
  wontfix-here on kazi-org/kazi]

- [ ] T70.12 Fix #1691 -- the Claude Code plugin published at
  kazi-org/claude-plugins is pinned at 1.250.0 while the binary line is at
  v1.275.1 (25 releases ahead); `claude plugin update kazi@kazi` reports
  "already at the latest version," which is misleading (the plugin itself
  is stale, not the operator's install). Bump the plugin version as part of
  the release pipeline: either (a) a release-please/CI step that bumps
  kazi-org/claude-plugins' manifest version on every kazi release touching
  skill/hook/MCP-affecting paths, or (b) at minimum a documented manual
  cadence with a CI check that fails when the gap exceeds N releases. If
  plugin releases are intentionally decoupled from binary releases,
  distinguish "a newer plugin exists -- update" from "the plugin is at its
  latest -- skew is expected" in the skew warning text instead of always
  prescribing a now-or-later no-op update.  Owner: pool  Est: 3h
  kind: agent  verifies: [infrastructure]  deps: []
  acc: [the plugin manifest version in kazi-org/claude-plugins is bumped by
  the release pipeline (or a CI check fails when it lags the binary line
  past a documented threshold) verified against a fixture/dry-run release;
  the skew-warning text distinguishes an available update from expected
  decoupled-release skew]

- [x] T70.13 Fix #1705 -- installing the kazi Claude Code plugin
  unconditionally wires all three session-bus delivery hooks (`SessionStart`,
  `UserPromptSubmit`, `Notification` -> `kazi bus hook <event>`) per
  ADR-0077 decision 4 ("installing the plugin IS the opt-in"). On a machine
  where the bus is retired/never adopted but the plugin is kept for the
  skill/MCP server, every session pays a live daemon's full board/digest
  injection at `SessionStart` (measured 13.6KB / ~3.4k tokens) with no
  opt-out short of uninstalling the whole plugin. Gate `kazi bus hook
  <event>` itself (the command both the plugin's hooks and `kazi
  install-hooks`' registrations invoke) behind an explicit additional
  opt-in that defaults OFF: `KAZI_BUS_HOOKS=1` env var or a marker file
  (`~/.config/kazi/bus-hooks-enabled`). `kazi install-hooks` writes the
  marker on install and removes it on `--uninstall`, so its existing
  explicit-consent flow keeps working with no second manual step; a
  plugin-only install (no `install-hooks` run) gets neither signal, so its
  bundled hooks stay silent until the operator opts in. This narrows
  ADR-0077 decision 4 in this one respect only; the skill/MCP/lockstep
  versioning are unaffected -- note the narrowing in ADR-0077's Consequences
  section rather than silently diverging from it.  Owner: pool  Est: 2.5h
  kind: agent  verifies: [infrastructure]  deps: []
  acc: [kazi bus hook <event> is a silent no-op when neither KAZI_BUS_HOOKS=1
  nor the marker file is present (pinned); kazi install-hooks sets the
  marker on install and removes it on --uninstall (pinned); a
  plugin-only install fixture (hooks invoked, no install-hooks run) never
  triggers a bus/daemon injection (pinned); ADR-0077 Consequences updated]
  Done: 2026-09-03 (PR #1706 "bus hooks: require an opt-in gate independent
  of plugin install", merged 2026-09-03T04:46:47Z, merge commit bbb40470;
  `Kazi.Bus.HookGate` implements the KAZI_BUS_HOOKS=1 / marker-file gate
  exactly as scoped; `kazi install-hooks` writes/removes the marker.
  Landed as a dedicated new ADR-0084 rather than an ADR-0077 Consequences
  amendment -- a reasonable divergence from the task's literal prescription,
  not a scope miss. #1705 auto-closed on merge. Verified via `gh pr view
  1706` + `gh issue view 1705`; commit fetched to origin/main, not yet
  merged into this local checkout's main at verification time.)

- [ ] T70.14 Fix #1708 -- `apply --parallel` run without `--workspace`,
  against a proposal whose goal has no `scope.workspace`/`scope.repo` set
  (`mode: :create`), crashes the ENTIRE OTP application: `Kazi.Scheduler.
  Partitioner.partition/3`'s only clause (partitioner.ex:80-85) guards
  `when is_list(goals) and is_binary(workspace) and is_list(opts)` -- a
  `nil` workspace fails the guard, there is no fallback clause, and the
  resulting `FunctionClauseError` is uncaught anywhere upstream, so the
  whole node terminates (`Kernel pid terminated`, an Erlang crash dump
  written to `~/.kazi/crash/`) instead of a clean CLI error. Fix: validate
  BEFORE the goal list reaches the scheduler -- at CLI-arg-parsing or
  proposal-approval time, a goal whose scope carries no workspace/repo AND
  no `--workspace` flag was given on the invocation is a clear, named
  validation error ("this goal has no workspace in its scope; pass
  --workspace <path>"), never a partitioner crash. Add a guarded fallback
  clause to `partition/3` itself as defense in depth (translate a `nil`
  workspace into the same clean error rather than crashing), independent
  of where the primary validation lands.  Owner: pool  Est: 3h  kind: agent
  lane: agent  verifies: [infrastructure]  deps: []
  acc: [kazi apply <ref> --parallel with no --workspace against a fixture
  goal whose scope.workspace and scope.repo are both nil exits with a
  clean, named validation error and no crash dump, in a fixture pinning
  the exact #1708 repro shape (red on pre-fix main: full VM crash); the
  same invocation WITH --workspace still starts cleanly (no regression)]

- [ ] T70.15 Fix #1707a -- `kazi integrate`'s generated PR title/body lists
  ONLY the predicates that converged, with zero indication that other,
  non-converged predicates exist in the goal: `passed_predicates/1`
  (integrate.ex:480-484) filters to `:pass` results only, and that filtered
  list is the SOLE input to both the PR title (`predicate_list(passed)` at
  integrate.ex:465) and the body's "Converged predicates:" line
  (integrate.ex:380,460). Observed: a 9-predicate goal (7 pass, 2 in
  `:error`) opened a PR titled/bodied as if it were a clean 7-predicate
  goal -- a caller reading the PR alone (title, body, `mergeStateStatus`)
  has no way to know 2 predicates were dropped. Same "kazi's own reported
  surface lies by omission" class as T70.1/T70.2/T70.3, on the PR artifact
  instead of the JSON status. Fix: the PR title/body must always account
  for EVERY predicate in the goal's vector, not just the passing subset --
  either list every predicate with its terminal status, or (minimum bar)
  an explicit "N predicates NOT included below because they did not
  converge" line naming them. Note this can only fire for a goal that
  reaches Integrate at all (per integrate.ex's own `landed`-gates-tree
  invariant) with a MIXED vector -- confirm at implementation time whether
  that shape is reachable through the normal `:converged` path or only via
  a caller invoking Integrate directly on a partial vector, and document
  which.  Owner: pool  Est: 3h  kind: agent  verifies: [infrastructure]
  deps: []
  acc: [a fixture Integrate call with a mixed predicate vector (some pass,
  some not) produces a PR title/body that names every predicate and its
  status, or an explicit count+list of omitted ones -- never silently
  truncated to the passing subset (pinned, red on pre-fix main reproducing
  the exact #1707a PR-title shape)]

- [ ] T70.16 Fix #1707b -- `swift_test`/xcodebuild capability predicates
  cannot execute at all inside `--parallel`'s ephemeral `--workspace`
  worktree: no `.xcresult` bundle is ever produced (repeated "file doesn't
  exist" for the expected result path), landing the predicate in `:error`
  -- distinct from and in addition to the already-fixed #1406 vacuous-green
  class (that was a wrong-verdict-on-a-result; this is no-result-at-all).
  A goal mixing a Go (or other CLI-testable) predicate with a
  `swift_test`/xcodebuild predicate reports the Swift side permanently
  stuck on environment capability alone, independent of code correctness.
  Investigate whether the ephemeral partition worktree can be made
  Xcode/simulator-capable (likely infeasible for a throwaway git-worktree
  checkout -- simulator state, DerivedData, and signing are not
  worktree-portable) or whether the correct fix is DETECTION + ROUTING: a
  goal containing a `swift_test`/xcodebuild predicate is flagged at
  `--parallel` dispatch time and either routed to a different execution
  path (a real subagent with actual Xcode/simulator access, not the
  ephemeral scheduler partition) or refused with a clear "this predicate
  type is not supported under --parallel" error -- never left to grind
  fruitlessly to `:error`/stuck. Document the decision and the resulting
  constraint in `kazi help apply` and AUTHORING.md regardless of which
  path is chosen.  Owner: pool  Est: 4h  kind: agent  lane: agent
  verifies: [infrastructure]  deps: []
  acc: [a fixture goal containing a swift_test/xcodebuild predicate, run
  under --parallel, either converges that predicate (if made capable) or
  is refused/routed with a clear, documented error at dispatch time --
  never silently grinds to :error/stuck with no explanation (pinned,
  reproducing the exact #1707 repro shape); the constraint is documented]

## Waves

- **Wave E70-A (P0, false-verdict correctness on `--parallel`, 6 agents,
  independent):** T70.1 (#1694 match_count false-pass), T70.2 (#1703
  HeartbeatTicker crash + false converged), T70.3 (#1696 false stuck via
  `:workspace`), T70.5 (#1698 disk-pressure worktree placement -- P0 given
  the confirmed live fleet incident), T70.14 (#1708 VM crash on missing
  --workspace), T70.15 (#1707a integrate PR silently omits failing
  predicates). These are "kazi's own reported verdict lied" bugs (plus one
  hard crash) -- the single worst class this project can ship, matching
  E69 Wave A's own #1679 in severity.
- **Wave E70-B (reliability + governance, 4 agents remaining, independent):**
  T70.4 (#1699 launcher-liveness fix), T70.6 (#1697 orphan reaping, compounds
  T70.5), T70.9 (#1695/#1704 `[scope]` enforcement), T70.16 (#1707b
  swift_test/xcodebuild under --parallel). T70.13 (#1705 bus-hook opt-in
  gate) is DONE (PR #1706, 2026-09-03).
- **Wave E70-C (authoring hygiene + process + release plumbing, 5 agents,
  independent):** T70.7 (#1690/#1693/#1701 predicate-lint hazards), T70.8
  (#1700 vitest doc fix), T70.10 (#1702 remove roadmap-note PR), T70.11
  (#1692 disposition, no code), T70.12 (#1691 plugin version bump).
- **Combined with E69:** a pool session finishing an E70 wave should pull
  from E69's still-open Wave A/B/C next (or vice versa) -- both epics are
  fully independent of each other; there is no ordering requirement between
  them, only within each epic's own waves.

## Notes

- 2026-09-03 (/plan refinement pass): T70.13/#1705 confirmed DONE -- PR
  #1706 merged 2026-09-03T04:46:47Z (commit bbb40470, fetched to
  origin/main), #1705 auto-closed on merge. Ticked here; Wave E70-B now 4
  remaining agents, not 5.
- Triage disposition 2026-08-30: operator decided 3 forks before task
  authoring -- #1692 REDIRECTED (no kazi-repo task, ADR-0067 pt 6 boundary
  respected), #1702 auto roadmap-note PR REMOVED entirely (not gated), #1699
  launcher-liveness check FIXED (not merely documented).
- #1707 and #1708 opened during this epic's own write window (a shared-
  machine disk-pressure incident delayed the write by roughly an hour) and
  were caught by a re-check of open issues immediately before writing, not
  missed by the triage. Folded in as T70.14-T70.16 rather than a separate
  epic -- both are freshly-filed, well-diagnosed, immediately-actionable
  bugs in the same "kazi's own reported surface lies" and "kazi's own
  runtime crashes instead of erroring cleanly" families this epic already
  covers, not a new theme.
- #1690 was already an open, undecided issue before this triage (not part
  of E69's 14); it is folded into T70.7 alongside its two close relatives
  (#1693, #1701) discovered in the same 2026-08-29/30 window, since all
  three are the same "a shell/regex idiom for counting test matches
  silently interacts badly with a runner default" family and share one
  `kazi lint` implementation.
- #1694/#1696/#1698/#1699/#1703 were all observed on released burrito
  binaries (1.273.7/1.275.0/1.275.1) under real `--parallel` fleet load,
  several during the SAME 2026-08-29/30 disk-pressure incident that also
  produced #1697/#1698 -- treat T70.1/T70.2/T70.3/T70.5 as one soak-tested
  cluster: after each lands, re-run the ORIGINAL repro shape (a real
  multi-partition `--parallel` goal against a real repo), not only the
  ExUnit pin, before closing its issue.
- ADR-0085 number collision risk with E69's T69.12 ("~0084") is called out
  above and in ADR-0085 itself -- whoever executes either task first should
  update the other epic file's reservation note once the real number is
  known, so a second session does not double-claim a taken slot.
