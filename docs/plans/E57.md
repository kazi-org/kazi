# E57 -- Predicate correctness: landing routing, guard/acceptance flags, vacuous-convergence follow-up (P1)

Back-link: [docs/plan.md](../plan.md) (master index). Sibling epics: [plans/](.)

fidelity: executable

Three real correctness bugs found via a full open-issue triage pass
(2026-07-17), all affecting the trustworthiness of a converged verdict --
the exact property kazi exists to guarantee. Two are clear, scoped bugs
(T57.1, T57.2). The third (#924/#1128) needed disambiguation before
scoping: #924's OWN triage comment already named the accepted fix direction
as ADR-0064 scenario predicates (epic E49, now fully shipped) -- NOT the
`kind: capability|guard` field #1128 proposes as an alternative, competing
mechanism. Building #1128 blind would risk shipping a second, overlapping
solution to a problem E49 already solves differently. T57.3 is scoped as a
verification-then-recommend task, not a build task, per this epic's own
no-scope-expansion discipline.

Acceptance: the serial `kazi apply` path routes a failing LANDED predicate
through the controller's own `Integrate` action exactly like the parallel
path already does (T57.1); `kazi plan --json --predicates` preserves a
caller-supplied `guard: true` independently of `acceptance` (T57.2); #924's
original failure mode is re-attempted with scenario predicates and the
result (fixed, or still reproducible) is recorded with a concrete
recommendation on #1128 (T57.3).

## Workstream A -- landing and predicate-flag bugs

- [ ] T57.1 Serial `kazi apply` routes a failing landed predicate to agent re-dispatch, not controller Integrate (#1290): found live during T44.14's dogfood (PR #1281, two real `kazi apply --harness claude` runs against a throwaway fixture, `mode = "pr"`). The whole point of E44 (T44.1-T44.13) is controller-owned landing -- the inner agent does the code work, kazi's OWN `Integrate` action (T44.3, "verifies-then-ships") pushes the branch and opens the PR, never a silent bulk commit, never the agent doing the landing itself. In the SERIAL, single-workspace `kazi apply` path specifically (the `--parallel` path already gets this right), a failing LANDED predicate (one that regressed after Integrate already shipped the branch/PR) is currently routed back to agent re-dispatch instead of through Integrate again. Root-cause the routing/decide logic (likely in the same decision function E44 built) and fix so the serial path's landed-predicate-regression handling matches the parallel path. Per ADR-0055 discipline: ride the EXISTING failing-predicate evidence machinery already in `decide/2` -- do NOT add a new special case to route this correctly; if the fix requires a special case, that is a signal the root cause is elsewhere and needs more investigation before coding.  Owner: pool  Est: 3h  kind: agent  verifies: [UC-057]  deps: []  acc: ExUnit -- a seeded serial-apply run with a LANDED predicate that regresses on a later iteration routes to Integrate (branch push + PR with the verification report), not to a fresh agent dispatch; the same scenario on the `--parallel` path (already correct) stays green as a regression pin; `decide/2` gains no new special-cased branch for this routing (verified by reading the diff, not just passing tests).
- [ ] T57.2 `kazi plan --predicates`: caller-supplied `guard: true` silently flattened to `guard: false` when `acceptance` is also set (#1356): reproduced via `kazi plan --json --predicates '<payload>'` with `{"acceptance": true, "guard": true, ...}` -- the emitted proposal comes back `"guard": false, "acceptance": true"`. Also reproduced with the ordering reversed (`acceptance: false, guard: true`) and with `--replace` -- guard is dropped regardless. Guards are load-bearing (they gate merge/ship decisions per the schema's own documented contract), so this is a data-loss-shaped bug, not cosmetic. Root-cause in the predicate-payload normalization path (likely a boolean-precedence bug where `acceptance` and `guard` are treated as mutually exclusive when they should be independent flags) and fix so both flags round-trip exactly as supplied.  Owner: pool  Est: 1.5h  kind: agent  verifies: [infrastructure]  deps: []  acc: ExUnit -- a predicate payload with `acceptance: true, guard: true` round-trips through `kazi plan --json --predicates` with BOTH flags preserved (regression pin, both orderings tested); `acceptance: false, guard: true` also round-trips correctly; `--replace` preserves the same invariant; existing predicate-payload tests stay green.

## Workstream B -- vacuous-convergence follow-up (#924/#1128 disambiguation)

- [ ] T57.3 Re-verify #924's vacuous-convergence failure mode against shipped E49 scenario predicates; recommend disposition of #1128 (#924, #1128): E49 (scenario predicates, demonstrate-then-pin, ADR-0064) is now fully shipped (docs/plans/E49.md, all tasks [x] except the `Owner: TBD` live dogfood T49.13). #924's own triage comment named scenario predicates as the accepted fix direction and said to keep the issue open "until E49 Wave A ships and this incident class is reproducible" -- that gate has now passed. Re-attempt (or construct a fixture reproducing) #924's original failure shape -- a UI+backend+integration feature whose custom_script/grep predicates all pass while the underlying feature is largely unbuilt -- but author it as scenario predicates (E49's mechanism) instead of raw custom_script/grep. Record whether scenario predicates actually close this failure class (they should, by construction: a scenario predicate can only pass by replaying a validated pin through a real surface provider, not by string-matching). Write the finding to docs/devlog.md. Based on the finding, post a concrete recommendation on #1128 (superseded/closed if scenario predicates cover the gap for goals that adopt them; or, if #1128's ask is specifically about goals that still use raw custom_script/grep predicates and choose NOT to adopt scenario predicates, scope #1128 as a narrower, still-valid guard-rail for that remaining case -- do not build #1128's `kind` field speculatively before this disposition is decided). Do NOT build the `kind: capability|guard` field as part of this task -- that decision is #1128's, made AFTER this verification, and may become its own future task if the recommendation says "still needed."  Owner: pool  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: docs/devlog.md carries a dated entry with the reproduction attempt, the scenario-predicate fixture used, and the observed result (closed or still-reproducible, with evidence either way); a comment is posted on GitHub issue #1128 with the concrete recommendation and the evidence link; issue #924 is closed with the finding if scenario predicates are confirmed to close the gap, or left open with an explanation if not.

## Wave plan

Single wave -- T57.1 and T57.2 are independent bug fixes in different code
paths (apply/decide logic vs. plan/predicates normalization). T57.3 is
independent research/verification work, no code dependency on the other two.
All three can dispatch together.

## Risk register

| id | risk | likelihood | impact | mitigation |
|---|---|---|---|---|
| R-E57-1 | T57.1's root-cause investigation finds the serial/parallel divergence is deeper than a routing bug (e.g. the serial path never built an Integrate-equivalent hook at all). | Med | Med | Scope stays "fix the routing to match parallel's already-correct behavior" -- if that turns out to require building new machinery, stop and flag rather than silently expanding scope (per CLAUDE.md's no-unilateral-scope-expansion rule). |
| R-E57-2 | T57.3 finds scenario predicates do NOT fully close #924's failure class (e.g. a gap in an edge case E49 didn't cover). | Low | Low | The task explicitly allows either outcome and requires evidence either way -- a "still reproducible" finding is a valid, useful result, not a task failure. |
