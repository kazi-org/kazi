# E59 -- Concurrency, multi-process safety, and CI flakiness (P1)

Back-link: [docs/plan.md](../plan.md) (master index). Sibling epics: [plans/](.)

fidelity: executable

Five issues found via a full open-issue triage pass (2026-07-17), all in the
"more than one thing running at once" family -- the standing assumption that
multiple kazi processes (or multiple test runs, or multiple CI jobs) share
state and must not corrupt each other. Two (#1019, #1255) are ALREADY
PARTIALLY FIXED per their own triage comments, left open pending live
verification -- this epic closes that verification gap rather than
re-investigating from scratch. Two (#1025, #1186) are CI test-flake clusters
that may share a root cause -- scoped as one investigation feeding two
possible fix tracks rather than assumed-separate from the start. One (#937)
is a large, partially-addressed proposal that needs its full gap inventory
read before scoping -- deliberately NOT pre-decomposed here beyond the first
task, per the rolling-wave depth policy.

Acceptance: the serial `kazi apply` path either has documented isolation
parity with `--parallel`, or the remaining gap is explicitly scoped as
follow-up tasks with named owners; #1019's migration-lock fix holds under a
REAL mixed-version release-window reproduction, not just code review;
#1025/#1186's flaky tests are root-caused (shared cause or not) and fixed
without weakening any real assertion; #1255's original `kazi version` hang
trigger is either reproduced and fixed, or downgraded to an explicit
monitoring/detection task with a stated reason it could not be reproduced.

## Workstream A -- serial-apply isolation gap (#937)

- [x] T59.1 Read #937's full gap inventory (all 6 comments) and scope the remaining tasks: the issue was filed after three prior incidents (#786 fixed, #924, #936) plus a fourth previously-unfiled gap that turned out to be the root cause of a real data-loss incident. Gap A (the SERIAL `apply` path has none of E21's `--parallel` isolation) is PARTIALLY fixed already (#940/v1.115.0 -- an executing apply refuses a workspace that IS a git repo's primary worktree root, with `--allow-primary-workspace` to override). A LATER comment documents a SECOND live incident, WITHOUT deletion this time -- cross-goal file bleed between two concurrent runs on the same non-primary worktree, meaning Gap A's fix does not cover this case. Read every comment in full (do not scope from the issue body alone -- the real gap inventory is in the comment thread) and produce a concrete task list for the remaining gaps as a follow-up to this task (append new `- [ ]` tasks to this workstream in the SAME PR that closes T59.1, following the rolling-wave depth policy -- this task's OWN acc is "the gap list exists and is concrete," not "the gaps are fixed"). Do not silently drop a gap because it looks hard -- if a gap is out of scope for this epic, say so explicitly with a reason.  Owner: pool  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: a dated docs/devlog.md entry lists every distinct gap named across #937's issue body and all 6 comments (not just Gap A), each gap marked fixed/partially-fixed/open with evidence; for every gap marked "open," a new checkable task is appended to this workstream in the same change with its own acc criteria; no gap is silently omitted from the list.  Done: 2026-07-17 (PR #1370; devlog entry "T59.1: #937 full gap inventory" dated 2026-07-17 inventories the body + all 6 comments; open gaps appended as T59.6-T59.10 below)

<!-- T59.6-T59.10 appended by T59.1 (2026-07-17): the still-open #937 gaps, one task per open gap. See the "T59.1: #937 full gap inventory" devlog entry for the fixed/partial/open evidence behind each. -->

- [x] T59.6 Give serial `kazi apply` the worktree indirection the parallel path already has (#937 Gap A1): a plain `kazi apply <goal-file> --workspace <path>` still mutates `<path>` directly -- only the primary-worktree *refusal* (#940, `runtime.ex:150`) and the never-`reset`/`clean` landing (`serial_landing.ex`) shipped; kazi does not own an isolated working directory for a serial run. Treat a serial goal as the 1-partition degenerate case of the scheduler so kazi MATERIALIZES a linked worktree (reuse `Kazi.Scheduler.Worktree.wrap/2` + `worktree_table.ex` survival, exactly as `--parallel` does) and `--workspace` means "the branch/base to integrate onto," not "the directory I edit in place." Preserve the existing literal-directory behavior behind the current `--allow-primary-workspace`/an explicit opt-out for callers who deliberately want in-place edits. Owner: pool  Est: 5h  kind: agent  verifies: [infrastructure]  deps: []  acc: ExUnit -- a serial `apply` against a goal with no `[[group]]` partitions creates exactly one linked worktree (assert via `git worktree list` in-test) and never writes the passed `--workspace` tree directly; untracked files in the passed workspace that are outside the goal's diff are byte-identical before and after the run; the in-place path still works when the opt-out flag is passed, pinned by a regression test.  Done: 2026-07-18 (PR #1436; the worktree indirection itself shipped under T50.1/T50.2/T54.1 (ADR-0065) -- `run_goal_serial` defaults to `Kazi.Scheduler.Worktree.wrap/2` off `--workspace`'s HEAD with `worktree_table` survival, lands via `SerialLanding`, and `--in-place` is the opt-out. This task closes the last literal acc gap by pinning an in-test assertion that a partition-less serial run materializes EXACTLY ONE linked worktree DURING the run (captured mid-dispatch via `git worktree list`) and never writes the passed `--workspace` tree, alongside the untracked-file byte-identity and `--in-place` regression tests in `serial_worktree_indirection_test.exs`.)

- [x] T59.7 Refuse a run whose `--workspace` is already in use by a DIFFERENT live run (#937 Gap G): `guard_no_live_duplicate/2` (`runtime.ex:302`, #942/#944) only refuses a second apply of the SAME goal_ref; it does not notice N *different* goals dispatched against one shared worktree (comment 2 & 4's incidents -- each goal's landed gate degraded to "has every other goal also finished," plus cross-goal commit bleed). Add a workspace-collision guard: refuse to start when the resolved working directory is already leased/held by a live run for a DIFFERENT goal (reuse the same fresh-heartbeat registry/lease staleness logic `guard_no_live_duplicate` uses; a stale/dead holder never blocks), with an explicit override flag mirroring `--allow-duplicate-run`. Largely obviated for the common case once T59.6 lands (kazi allocates its own worktree), so scope this as the guard for callers who still pass an explicit shared `--workspace`. Owner: pool  Est: 3h  kind: agent  verifies: [infrastructure]  deps: []  acc: ExUnit -- with a fresh-heartbeat registry row for goal X holding workspace W, starting goal Y against W is refused with a clear error naming the live holder; the override flag lets it proceed; a stale/dead holder row does NOT block; a second run of the SAME goal still hits the existing duplicate-run guard unchanged (no regression to #942/#944).  Done: 2026-07-18 (PR #NNNN; `guard_no_workspace_collision/3` in `runtime.ex` refuses a fresh-heartbeat LIVE run for a DIFFERENT goal_ref holding the same resolved workspace via `RunRegistry.list_live/0`, override `--allow-workspace-collision` (4-place registration), documented in ADR-0065 decision 6 + AGENTS.md/SKILL.md; pinned by `runtime_workspace_collision_test.exs` (5 tests, incl. the #942/#944 same-goal regression pin)).

- [ ] T59.8 Make authored-path staging the default so an unscoped goal cannot sweep a sibling's WIP (#937 Gap A4, comment 5's commit-boundary-corruption incident): `integrate.ex` `stage_all/2` (#819) already scopes staging for a goal that declares `[scope] paths` (`git add -u` + `git add -- <paths>`), but a goal with NO declared paths still falls back to whole-workspace `git add -A` (`integrate.ex:512`), so an unscoped goal sharing a tree can still absorb another goal's uncommitted files into its own commit. Close the remainder: either (a) stage only the files the harness actually authored this run (the run's own diff/touched-file list) rather than `-A`, or (b) when the workspace is shared/leased by another live run, refuse the blind `-A` and require scoping. Do not change behavior for a solo goal in its own isolated worktree (post-T59.6 the common case), where `-A` is safe. Owner: pool  Est: 4h  kind: agent  verifies: [infrastructure]  deps: []  acc: ExUnit -- a goal with no declared `[scope] paths` running in a tree that also contains an unrelated untracked/uncommitted file does NOT stage that file into its commit (assert the commit's file list excludes it); a solo goal in an isolated worktree still commits its full authored diff (no regression); the chosen guard is documented in the integrate moduledoc.

- [ ] T59.9 Verify `--parallel` materializes one linked worktree per partition and surface isolation in `--explain`/`--check` (#937 Gap F): comment 3 (v1.127.0) observed 9-10 agents sharing ONE cwd for a `[[group]]`-only goal. The worktree-per-partition machinery is present now (`Worktree.wrap/2` + `leased_reconciler.ex` + `worktree_table.ex`, T21.4/T54.1), but two things are unconfirmed: (a) a `[[group]]`-only goal of that exact shape actually materializes N distinct worktrees end-to-end, and (b) a caller can CONFIRM the per-partition isolation BEFORE a long grind. Add a test proving N partitions => N worktrees, and surface the planned per-partition worktree/isolation in `--explain` (and/or `--check`) output so isolation is inspectable up front rather than discovered via `ps`/`git worktree list` mid-run. Owner: pool  Est: 3h  kind: agent  verifies: [infrastructure]  deps: []  acc: ExUnit -- a goal with K disjoint `[[group]]` partitions run under `--parallel` produces K distinct linked worktrees (assert via the worktree table / `git worktree list`), none of them the passed `--workspace` root; `kazi apply --explain` on that goal reports the per-partition worktree/isolation plan (assert the K partitions each show an isolated working dir), pinned as a regression test; docs for `--explain`/`--check` mention the isolation surface.

- [ ] T59.10 Guard against whole-tree-clean acceptance predicates that are unsafe in a shared workspace (#937 Gap E): comment 2 showed a `*-landed` predicate shaped as `test -z "$(git status --porcelain)"` giving false-negative verdicts while a sibling goal committed in the same tree. T54.1 fixed the built-in serial *landing* gate (branch-identity, not tree-clean), but a goal AUTHOR can still hand-write a `git status --porcelain`-empty *acceptance* predicate that is structurally unsafe under any multi-goal-same-workspace scheduling, and nothing warns them. Add an authoring clarify-floor warning (extend `authoring/clarify.ex`, the same tier that flags vacuous grep predicates) that flags a custom_script predicate whose command tree-scans `git status --porcelain` (or equivalent whole-tree cleanliness) as unsafe under shared-workspace scheduling, recommending scoping to the goal's own paths / relying on kazi's identity-based landing; document the hazard in the predicate-authoring docs. Low surface; complements the isolation tasks T59.6/T59.7 rather than replacing them. Owner: pool  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: ExUnit -- a goal-file carrying a `git status --porcelain`-empty acceptance predicate surfaces a clarify-floor warning naming the shared-workspace hazard; a predicate scoped to the goal's own paths does NOT trigger it; the hazard + the safe alternative are documented in the predicate-authoring docs (assert the doc section exists).

## Workstream B -- verify already-landed fixes hold live (#1019, #1255)

- [x] T59.2 Verify #1019's migration-lock fix under a real mixed-version release window: commits `1e4cccb` (bounds the migration-lock wait instead of hanging) and `4352f24` (refuses to migrate a schema newer than the running binary knows) already landed on main per the issue's own triage comment, but the issue stays open pending LIVE verification across a real mixed-version scenario (multiple kazi versions concurrently touching the shared `~/.kazi/kazi.db` during an actual release/self-update window, not just a synthetic fixture). Reproduce the original observed conditions as closely as practical (multiple long-lived kazi processes on different versions holding the same db file simultaneously) and confirm neither the migration-lock deadlock nor the 0%-CPU startup hang recurs. If it DOES still recur under some variant, root-cause and fix that specific gap before closing.  Owner: pool  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: docs/devlog.md records the live reproduction attempt (versions used, process overlap achieved, duration) and its outcome; if clean, issue #1019 is closed with this evidence linked; if not clean, a new task is added here with the specific remaining failure mode and the issue stays open with that finding posted as a comment. Done: 2026-07-17 (PR #1373; live-verified mixed-version window v1.195.0 + v1.212.0 across a real 2-migration schema gap -- refuse-newer-schema degrades cleanly sequentially and under 6-way concurrent contention, no migration-lock deadlock, no 0%-CPU hang; #1019 closed on this evidence; see devlog "T59.2: #1019 migration-lock fix holds under a live mixed-version window")
- [x] T59.3 Reproduce or retire #1255's original `kazi version` hang trigger: PR #1266/v1.186.1 already bounded every `Kazi.Bus` call in `with_conn/2` with a hard deadline (fixing a confirmed, independently-verified bug in the "housekeeping must never hang" family) -- but the issue's own author explicitly left it open because the bus fix does not explain the ORIGINAL specimen (a `kazi version` process hanging immediately after a Homebrew upgrade, printing burrito's old-version cleanup lines and then never proceeding to print the version string). Attempt to reproduce the ORIGINAL trigger (a `kazi version` invocation immediately following a version upgrade, ideally via the same Homebrew-upgrade path) with a native stack sample (per the issue's own investigation notes) and `lsof` for DB/network file descriptors if it hangs. If reproducible: root-cause and fix the specific `kazi version`-path hang (distinct from the already-fixed bus path). If NOT reproducible after a real, documented attempt: downgrade this to a monitoring/detection task -- e.g. a startup watchdog that surfaces WHERE a hang is stuck (which subsystem, which fd) so the NEXT specimen is diagnosable in minutes instead of requiring a fresh from-scratch investigation -- rather than leaving the issue open indefinitely with no forward motion.  Owner: pool  Est: 2.5h  kind: agent  verifies: [infrastructure]  deps: []  acc: EITHER a reproduced hang is root-caused and fixed with a regression test, OR docs/devlog.md documents a real reproduction attempt (specific steps tried, what did NOT hang) and a startup-hang watchdog/diagnostic mechanism ships instead, with its own test coverage; issue #1255 is closed or updated with whichever outcome occurred, evidence linked either way.  Done: 2026-07-17 (documented non-repro across all 3 variants incl. the exact `.burrito_live` cleanup-skip signature; the `version` path itself never hangs -- so shipped the fallback startup-hang watchdog `Kazi.StartupWatchdog` (dump-and-continue default, `KAZI_STARTUP_WATCHDOG_MS`/`_HALT`) wired into all 3 CLI entry points, with function-level test coverage; live-verified in the built release binary; #1255 updated. See devlog "T59.3: #1255 ... non-repro + startup watchdog".)

## Workstream C -- CI test-flake cluster (#1025, #1186)

- [x] T59.4 Investigate whether #1025 and #1186 share a root cause before scoping fixes: #1025 names 3 specific tests (`Kazi.Loop.WorkspacePrepTest`, an `EnforcementTest` await, `CLIPlanBudgetSuggestionTest`) flaking under full-suite load with SQLite "Database busy" / `assert_receive` timeouts, passing in isolation. #1186 names a DIFFERENT but overlapping cluster (~4 distinct tests observed over time, including `Kazi.Memory.SemanticIndexTest` and `Kazi.Loop.ContextStoreTest`) -- async tests asserting on global/instantaneous state, costing roughly one spurious CI failure per full run; main itself is green 10/10 recent runs, so this is PR-concurrency-induced test isolation, not a correctness regression. Both clusters smell like the same underlying defect (shared/global state -- SQLite connections, GenServer-registered names, or similar -- contended by concurrent test processes) rather than N independent test bugs. Read both issues' full comment threads (specific test names, specific timing evidence) and determine: same root cause or genuinely different ones. This task's acc is the finding, not the fix -- T59.5 depends on it.  Owner: pool  Done: 2026-07-17  Est: 2h  kind: agent  verifies: [infrastructure]  deps: []  acc: docs/devlog.md records, for every distinct flaky test named across both issues, whether its failure mode traces to the SAME contended resource (name it specifically -- e.g. "a single global SQLite connection pool size too small under N concurrent test processes") or a genuinely distinct cause; the finding states explicitly whether T59.5 should be one fix or several.
- [ ] T59.5 Fix the flaky-test cluster per T59.4's finding (#1025, #1186): implement whichever remediation T59.4's root-cause finding calls for -- likely one of: increase `assert_receive`/`assert_eventually` timeouts specifically for load-sensitive assertions (not a blanket global increase, which just hides the next flake slower), give the contended tests dedicated/sandboxed DB connections or GenServer names (Ecto Sandbox mode, unique process names per test), or serialize the specific contending tests (`async: false` on the minimal set that needs it, not the whole suite). Check this repo's existing memory/precedent for prior flaky-test fixes of the same shape before picking an approach (grep git log / docs/devlog.md for prior flake fixes). Do NOT weaken or remove any real assertion to make a test stop flaking -- the fix must address the timing/isolation cause, not the symptom.  Owner: pool  Est: 3h  kind: agent  verifies: [infrastructure]  deps: [T59.4]  acc: every test named in #1025 and #1186 passes reliably under a stress run (run the identified tests concurrently with the rest of the full suite, repeated at least 10x in CI or locally under load, zero flakes); no existing assertion was weakened (diff review confirms only timing/isolation mechanics changed, not assertion strength); the fix is documented (a code comment or devlog entry) so a future contributor understands WHY the isolation mechanism exists, preventing an accidental revert.

## Wave plan

Wave 1: T59.1, T59.2, T59.3, T59.4 (all independent investigation/verification
tasks, no shared state). Wave 2: T59.5, gated on T59.4's finding. Any new
tasks T59.1 appends to this workstream are a follow-up wave, scoped once
T59.1 lands.

## Risk register

| id | risk | likelihood | impact | mitigation |
|---|---|---|---|---|
| R-E59-1 | T59.1's gap inventory turns out large enough that fully fixing #937 is a multi-week epic on its own, not a single task's follow-up. | Med | Low | T59.1's acc explicitly only requires a concrete, honest task list -- not that every gap gets fixed in this epic. A large list is a valid outcome; splitting it into its own epic later is fine. |
| R-E59-2 | T59.3's reproduction attempt for #1255 fails to reproduce, and the fallback watchdog task turns out to be nontrivial (bigger than the 2.5h estimate). | Med | Low | The acc line already accepts "documented non-reproduction + a watchdog" as a valid outcome; if the watchdog itself is large, split it into its own follow-up task rather than blocking this epic's close. |
