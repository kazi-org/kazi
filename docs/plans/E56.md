# E56 -- Plan/lore/ADR doc hygiene sweep (P2)

Back-link: [docs/plan.md](../plan.md) (master index). Sibling epics: [plans/](.)

fidelity: executable

Four small, independent doc-correctness bugs found via a full open-issue triage
pass (2026-07-17): stale verb references in shipped example goal-files, a
self-teaching artifact that still hand-rolls a workaround for a verb that has
since shipped for real, an unguarded sequential-ID allocation race in
docs/lore.md that has already produced 7 duplicate pairs, and a real ADR
numbering collision (two files both claimed 0071) discovered during this same
triage pass and fixed inline as part of authoring this epic (see Progress Log
in docs/plan.md, 2026-07-17). None of these block a release; all of them erode
trust in the docs as a source of truth if left open.

Acceptance: `priv/examples/*.toml` carries no `kazi run` references (and a
guard makes that regression impossible); `kazi install-skill`'s generated
RECIPES.md teaches `kazi apply --check` instead of the pre-1.160 gate-variant
hack and drops the closed #936 caveat; docs/lore.md has zero duplicate L-NNNN
ids and a mechanism (script + CI check) that makes a future collision
structurally impossible, the same shape as the migration-number safety net
`/apply` already runs; the ADR-0071 duplicate is resolved (done inline this
session, see below) and no dangling reference to the old filename remains.

## Workstream A -- stale command references (#1242, #1166)

- [x] T56.1 Fix stale `kazi run` references in `priv/examples/*.toml` (#1242): four files (`standing_maintenance.toml`, `deploy_target.toml`, `browser_acceptance.toml`, `create_feature.toml`) have comments invoking the removed `kazi run` verb -- the shipped verb is `kazi apply`. Fix all 7 occurrences. The `doc command-accuracy` guard (T28.4) only scans `.md` files, which is why these slipped through silently -- extend the guard (or add a lightweight sibling check) to also scan `priv/examples/*.toml` comments for removed-verb references, so this class of drift can't recur.  Owner: pool  Est: 1h  kind: agent  verifies: [infrastructure]  deps: []  acc: `grep -rn "kazi run" priv/examples/*.toml` returns zero matches; the extended doc-command-accuracy guard fails CI on a synthetic `.toml` fixture carrying a `kazi run` comment (regression pin); the guard's existing `.md` behavior is unchanged (no false positives introduced).
- [x] T56.2 `kazi install-skill`'s generated RECIPES.md teaches the real observe-only verb (#1166): `lib/kazi/teach/install_skill.ex` still emits a hand-rolled "gate variant" recipe ("kazi has no observe-only verb (#805 context)... build a fake goal-file with `max_iterations = 1`, `command = "/usr/bin/true"`, strip `landed`") even though `kazi apply --check` shipped and IS the real observe-only verb. Also drop the closed-#936 split-goal-file caveat the same generated content still carries. Rewrite the relevant section(s) to teach `kazi apply <goal> --check` directly. Confirmed still live on current main (2026-07-17), not just the shipped binary.  Owner: pool  Est: 1.5h  kind: agent  verifies: [infrastructure]  deps: []  acc: a fresh `kazi install-skill` run's generated RECIPES.md contains no "no observe-only verb" / gate-variant-hack text and no #936 caveat; it teaches `--check` with a runnable example; the existing teach-coherence guards (`teach_coherence_test.exs`, `teach_coherence_reverse_test.exs`) stay green; a new pinned test asserts the gate-variant hack string is ABSENT from the generated output (regression guard).

## Workstream B -- sequential-ID collision hazards (#1220, ADR numbering)

- [ ] T56.3 docs/lore.md L-NNNN id collision fix + allocation guard (#1220): 7 duplicate id pairs exist on main today (L-0015, L-0019, L-0029, L-0035, L-0036, L-0041, L-0042) because nothing stops two concurrent pool tasks from picking the same "next" number -- the same sequential-numbering hazard the `/apply` teammate contract already guards for migrations ("do NOT pick the next number from your local tree -- parallel agents and merged-but-unpulled work make local numbering stale"), applied here to lore, where nothing guards it. Fix: (a) renumber the SECOND of each duplicate pair to the next free id above the current max, updating any place that cross-references the old id by number (grep the whole repo, not just docs/lore.md, before renumbering -- an id may be cited from a plan task or ADR); (b) add a script (`.github/scripts/` or `docs/lore.md`'s own tooling directory, matching this repo's existing script-location convention) that computes the next free id from the CURRENT git-fetched state of docs/lore.md (never local-tree-only) and a CI check that fails a PR introducing a duplicate id.  Owner: pool  Est: 2.5h  kind: agent  verifies: [infrastructure]  deps: []  acc: `grep -oE "L-[0-9]+" docs/lore.md | sort | uniq -d` returns empty after the fix; every renumbered id's cross-references (if any) are updated, not orphaned; a new CI check (or extension of an existing one) fails on a synthetic PR that reintroduces a duplicate id; the allocation script computes against fetched-remote state, not local HEAD (pin via a test that simulates a stale local tree).
- [x] T56.4 ADR-0071 numbering collision -- resolved inline (this epic's authoring session, 2026-07-17): two ADR files both claimed number 0071 (`0071-bus-delivery-is-installed-not-documented.md`, added 2026-07-16, and `0071-gherkin-runtime-provider.md`, added 2026-07-15 -- confirmed via `git log --diff-filter=A`). Renumbered the later-added file to `0076-bus-delivery-is-installed-not-documented.md` (0076 was the next free ADR number; 0071-0075 were all already taken). Fixed every cross-reference found via a repo-wide grep: `docs/session-bus.md`, `docs/adr/README.md`, `docs/plan.md`, `docs/plans/E51.md`, `docs/plans/E55.md`, `docs/adr/0072-bus-digest-protects-the-machine-path.md`, `docs/adr/0073-the-board-current-state-claims-identity.md`, `AGENTS.md`, `README.md`, `docs/devlog.md`. Deliberately did NOT touch `CHANGELOG.md`: it is a historical record and its old "ADR-0071" references (some of which, chronologically, actually meant the gherkin ADR at the time they were written -- e.g. the 2026-06-xx GherkinExpander entry) must stay exactly as originally written; rewriting past changelog entries to match a later renumbering is revisionist and was explicitly avoided.  Owner: pool  Est: 1h  kind: agent  verifies: [infrastructure]  deps: []  acc: `grep -rln "0071-bus-delivery-is-installed-not-documented" --include=*.md .` returns empty (file fully renamed, no dangling path references); `grep -rn "ADR-0071" --include=*.md .` matches ONLY `docs/adr/0071-gherkin-runtime-provider.md` (self-reference) and `CHANGELOG.md` (historical, untouched); `teach_coherence_test.exs`/`teach_coherence_reverse_test.exs` stay green after the AGENTS.md/README.md edits (verify with `mix test test/kazi/teach_coherence_test.exs test/kazi/teach_coherence_reverse_test.exs`).  Done: 2026-07-17

## Wave plan

Single wave -- all four tasks are independent (different files, no shared
state beyond the repo-wide grep discipline each already carries). T56.4 is
already done (landed inline while authoring this epic); T56.1-T56.3 dispatch
together.

## Risk register

| id | risk | likelihood | impact | mitigation |
|---|---|---|---|---|
| R-E56-1 | T56.3's lore renumbering misses a cross-reference to a duplicate id from outside docs/lore.md (a plan task, an ADR, a devlog entry). | Low | Low | The acc line requires a repo-wide grep before renumbering, not a docs/lore.md-only search. |
