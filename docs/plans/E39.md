### E39 -- Orchestrator-driving ergonomics: close the plan -> approve -> apply loop over `--json` (P1, ADR-0049)

The T15.9 nested-loop dogfood (orchestrator -> kazi -> a local model via opencode)
drove the full `plan -> approve -> apply` spine over `--json` and proved the inner
loop works, but surfaced four concrete points where kazi is awkward to drive as a
tool. This epic resolves them so an outer orchestrator can drive the spine end to
end with zero prose-scraping and zero goal-file reconstruction. Source friction is
recorded in `docs/devlog.md` (2026-06-28, T15.9); the decision is ADR-0049.

- [ ] T39.1 `kazi plan --json --predicates <json>` honors the caller's `goal_id` and `idea`: caller-drafts currently mints a generic `caller-supplied-predicates` id and `idea`, ignoring the fields the orchestrator supplied, so the authored goal cannot be named. Thread the supplied `goal_id`/`idea` through `Kazi.Authoring` into the persisted proposal and the `--json` result; fall back to the generated defaults only when absent.  Owner: pool  Est: 1h  kind: agent  verifies: [UC-033]  deps: []  acc: ExUnit -- `plan --json --predicates` with a `goal_id`/`idea` returns a proposal whose `goal_id` and `idea` equal the supplied values and persists them; omitting them keeps today's generated defaults. No prose-scraping.

- [ ] T39.2 `kazi apply <proposal-ref>` runs an APPROVED proposal directly (no goal-file): `apply` requires a goal-file on disk, but `plan`/`approve` never write one, forcing the orchestrator to reconstruct it. Teach `apply` to accept a `prop-...` ref: when the argument resolves to an `approved` proposal in the read-model, load that goal and run it; a non-approved or unknown ref errors clearly; a path argument behaves exactly as today (back-compat). Update `kazi help` + `kazi schema apply` + the orchestrator recipe doc.  Owner: pool  Est: 2h  kind: agent  verifies: [UC-033, UC-035]  deps: [T39.1]  acc: ExUnit -- `apply prop-<approved>` loads the approved goal and runs it (reuses the converge wiring); `apply prop-<proposed-not-approved>` and `apply prop-unknown` return a clear non-zero error; `apply <goal-file>` is unchanged. Docs updated in the same change (ADR-0034).

- [ ] T39.3 `kazi approve <ref> --write <path>` materializes the approved goal-file: for file-based / version-controlled workflows that WANT a goal-file artifact, `approve --write <path>` writes the approved goal as a loadable goal-file and (under `--json`) reports the written path. Complements T39.2 (ref-threading) without forcing either workflow.  Owner: pool  Est: 1.5h  kind: agent  verifies: [UC-033]  deps: []  acc: ExUnit -- `approve <ref> --write <path> --json` writes a goal-file that `apply <path>` loads and runs to the same result as `apply <ref>`; the `--json` object includes the written path; absent `--write`, behavior is unchanged. `kazi help`/`schema` updated.

- [ ] T39.4 `--json` stdout is the single JSON object on every entrypoint: under `--json`, dev/`mix run` co-mingles Phoenix/Ecto logs into stdout, so an orchestrator must select the JSON object out of log lines (the released binary is already clean). Route logs to stderr (or silence them) for `--json` runs across escript / `mix run` / release, so stdout decodes as exactly one object.  Owner: pool  Est: 1.5h  kind: agent  verifies: [UC-033, infrastructure]  deps: []  acc: ExUnit/integration -- a `--json` invocation under the noisy (dev-logger) path emits stdout that `Jason.decode/1` parses as a single object with NO leading/trailing log lines; stderr may carry logs; verified on at least the `mix run` path that exhibited the leak.

- [ ] T39.5 Authoring on the escript: degrade or guide clearly: the operator escript cannot author (`plan`/`approve` hard-fail "the read-model is unavailable; authoring requires persistence") because an escript cannot bundle the SQLite NIF. Either give authoring an ephemeral/in-memory read-model so the escript can `plan`/`approve`, OR make the failure name the supported entrypoint (release binary / `mix` path) and document it at `docs/` + `kazi help`. Pick the smaller correct option; record which in the task note.  Owner: pool  Est: 2h  kind: agent  verifies: [UC-033]  deps: []  acc: ExUnit -- either `plan`/`approve` succeed on the escript path (ephemeral store) and the result is correct, OR the failure message names the supported entrypoint and a doc/help line states it; no silent or cryptic failure. Docs updated (ADR-0034).

- [ ] T39.6 LIVE regression dogfood: re-drive the full nested loop over CLEAN `--json` with the fixes: re-run the T15.9 orchestrator -> kazi -> local-model loop end to end -- `plan --json` (caller-drafts, named goal) -> `approve --json` -> `apply <proposal-ref> --json --harness opencode --model <local>` -- with NO goal-file reconstruction and NO stdout log-scraping, parsing each result object and branching on `next_action`. Record the clean run (and the still-honest convergence caveat for the slow local model) in `docs/devlog.md`.  Owner: pool  Est: 1.5h  kind: any  verifies: [UC-033]  deps: [T39.1, T39.2, T39.4, T39.7]  acc: observed evidence in `docs/devlog.md` that the spine drives end to end over `--json` with zero goal-file reconstruction and zero prose-scraping (each step's stdout decodes as one object); with T39.7 the inner agent's edits land IN the workspace so convergence is now achievable (a wiring proof still counts if the local model is too slow). Requires a reachable local-model endpoint (live-capable session or operator).

- [ ] T39.7 opencode runs must isolate to the goal's `--workspace` (thread `--dir`): the T15.9 dogfood proved the inner local model SUCCEEDED at the fixture task but its edit (`hello.txt`) landed in opencode's OWN resolved directory (the git/server root), NOT kazi's `--workspace` -- so kazi's workspace-scoped predicate never saw it and the run never converged. `CliAdapter` sets `System.cmd(... cd: workspace)`, but `opencode run` does not honor the launch cwd (it resolves its project root / attaches to a persistent server); it exposes a `--dir <path>` flag ("directory to run in"). Thread the workspace into the opencode profile argv as `--dir <workspace>` (verify against the installed `opencode run --help`, currently v1.17.9) so the inner agent's edits land in the goal's workspace. Add a regression that the opencode profile argv includes `--dir <workspace>`.  Owner: pool  Est: 1.5h  kind: agent  verifies: [UC-032, UC-033]  deps: []  acc: ExUnit -- the opencode profile renders `--dir <workspace>` in its argv (workspace threaded from the run); a dogfood/integration check shows an opencode-driven edit landing INSIDE `--workspace` (and thus observable by a workspace-scoped predicate). `lib/kazi/harness/profiles/opencode.ex` updated; lore note if opencode's cwd behavior is version-sensitive.
