# Architecture Decision Records

Each ADR captures one decision: the context, the decision, the consequences, and
the alternatives rejected. ADRs are immutable once accepted — to change a
decision, write a new ADR that supersedes the old one (and update
[`../concept.md`](../concept.md)).

| ADR | Title | Status |
|-----|-------|--------|
| [0001](0001-positioning-outer-loop-reconciler.md) | Positioning: an outer-loop reconciler, not a harness | Accepted |
| [0002](0002-goals-as-predicates.md) | Goals are machine-checkable predicate sets | Accepted |
| [0003](0003-language-elixir-otp.md) | Runtime: Elixir / OTP + Phoenix LiveView | Accepted |
| [0004](0004-coordination-substrate-nats-jetstream.md) | Coordination substrate: NATS JetStream | Accepted |
| [0005](0005-data-layer-split.md) | Data-layer split (Git / JetStream / ETS / SQLite) | Accepted |
| [0006](0006-coordination-leases-and-graph-partitioning.md) | Coordination by resource leases + graph partitioning | Accepted |
| [0007](0007-build-strategy-walking-skeleton.md) | Build strategy: walking skeleton (idea → production) | Accepted |
| [0008](0008-harness-invocation-and-context.md) | Harness invocation: headless, stateless per iteration; kazi owns context | Accepted |
| [0009](0009-prompt-construction-thin-evidence-projection.md) | Prompt construction: a thin, deterministic evidence projection | Accepted |
| [0010](0010-context-injection-reexploration-mitigation.md) | Context injection to mitigate per-iteration re-exploration | Accepted |
| [0011](0011-slice3-operator-surfaces.md) | Slice-3 operator surfaces (LiveView dashboard + Telegram bridge) decoupled from the core loop | Accepted |
| [0012](0012-pluggable-retrieval-memory-adapter.md) | Pluggable semantic-retrieval memory adapter (off by default) | Accepted |
| [0013](0013-adopt-reverse-engineer-goals.md) | Adopting kazi on an existing project (`kazi init` reverse-engineers a goal-file) | Accepted |
| [0014](0014-binary-distribution-burrito-homebrew.md) | Binary distribution via Burrito + Homebrew (supersedes escript-as-distribution) | Accepted |
| [0015](0015-init-source-output-model-registry-goal-set.md) | Withdraw the capability-registry adapter; a future spec importer (OpenAPI/gherkin) instead | Accepted |
| [0016](0016-generic-harness-profiles.md) | Generic harness profiles: config-driven multi-harness support (opencode, Codex, ...) | Accepted |
| [0017](0017-automated-brew-release-pipeline.md) | Automated brew release pipeline (release-please -> CI build -> tap auto-bump) | Accepted |
| [0018](0018-website-stack-hosting-domain.md) | kazi website: Astro + Tailwind on GitHub Pages at kazi.sire.run | Accepted |
| [0019](0019-interactive-clarify-phase-for-propose.md) | Interactive clarify phase for `kazi propose` (hybrid question-gen, CLI-first, inline + `--adr` rationale) | Accepted |
| [0020](0020-hierarchical-predicate-grouping.md) | Hierarchical predicate grouping via a declared group taxonomy (referenced by id, validated at load; derived per-group budgets; Obsidian/Mermaid export) | Accepted |
| [0021](0021-intended-vs-actual-reconciliation.md) | Intended-vs-actual reconciliation: import intent from standard specs (OpenAPI/gherkin) + prose via the harness; detect dead code via a surface-coverage meta-predicate | Accepted |
| [0022](0022-harness-onboarding-conformance.md) | Onboarding any CLI coding harness: the profile conformance contract (non-TTY subprocess-safe, structured output) + the add-a-harness recipe; Codex/Antigravity/claw-code tiers | Accepted |
| [0023](0023-harness-friendly-agent-drivable-cli.md) | kazi as a harness-friendly, agent-drivable CLI: `--json` + non-interactive guarantees + a versioned result contract (kazi self-conforms to ADR-0022); orchestrator owns the two-tier model policy; `kazi propose` is the single agent authoring path | Accepted |
| [0024](0024-kazi-self-teaching-to-harnesses.md) | kazi is self-teaching to harnesses: an opt-in Claude Code skill (`kazi install-skill`) + `kazi help --json`/`schema` + `AGENTS.md` + a `kazi mcp` server, so an agent knows how to drive kazi | Accepted |
| [0025](0025-docs-lead-with-agent-driven-onramp.md) | Documentation and website lead with the agent-driven on-ramp (claude->kazi->cheap-harness); vanilla `kazi run` demoted to a reference tier; honest cost framing; sequenced after the install-skill/mcp on-ramp ships | Accepted |
| [0026](0026-kazi-under-apply-pool.md) | kazi UNDER `/apply --pool` (shape a): `/claim` stays outer task-selection, kazi blast-radius leases are inner coordination; caller-drafts bridges `acc:`->predicates; layered L1 gate -> L2 loop -> L3 leases (NATS) -> L4 observability; does not replace the pool | Accepted (parallelization stance superseded by 0027; retained as interop) |
| [0027](0027-kazi-owns-parallelization-native-scheduler.md) | kazi owns parallelization: a native scheduler partitions a goal-set by blast radius, leases each partition, and spawns N supervised concurrent reconcilers (in-memory lease single-node = NATS-free) to collective convergence; codifies `/apply --pool`+`/claim` into kazi; single-goal stays the serial on-ramp | Accepted |
| [0028](0028-dependency-aware-partitioning-predicate-graph-waves.md) | Dependency-aware partitioning ("predicate-graph waves"): declare `needs` edges between predicate groups; the scheduler executes the DAG topologically with blast-radius parallelism inside each frontier, objective-convergence gating, and pipelining (no barrier); codifies `/plan`'s `deps:` + `/apply`'s Waves | Accepted |
| [0029](0029-drop-telegram-bridge.md) | Drop the Telegram bridge -- the orchestrating agent (Claude) is the human's mobile interface, so a kazi-native chat surface is redundant; remove the bridge (no dep change), drop T20.9, keep the LiveView dashboard; headless-autonomous pings deferred to a future generic webhook (supersedes the Telegram part of 0011) | Accepted |
| [0030](0030-content-marketing-agent-native-positioning.md) | Content-marketing + agent-native positioning (research-grounded): lead every surface with the agent-drives-kazi paradigm + a human-noun/borrowed-frame tagline; hero = a transcript of the loop reaching objective-true; without/with before-after; agent-voiced proof + a memorable invocation; ONE recurring growth engine (a dogfood "done" leaderboard); HN-first launch kit (refines ADR-0025) | Accepted |
| [0031](0031-kazi-skill-router-subsumes-loop-apply-qualify.md) | The kazi skill as a ROUTER (`plan`/`apply`/`status`/`adopt` sub-skills; skill verbs map to CLI `propose`/`run`/`status`/`init`): `kazi apply` (CLI `kazi run`) subsumes loop+apply+qualify for code goals; `/plan` re-seated as the intent-authoring layer that emits a goal-set; `/tidy` kept as hygiene; subsumption messaging gated on the E21/E23 dogfoods (refines ADR-0024) | Accepted (verb-map superseded by 0032) |
| [0032](0032-rename-cli-verbs-run-apply-propose-plan.md) | Rename the CLI verbs `run`->`apply`, `propose`->`plan` (+ `mix kazi.run`->`mix kazi.apply`) so human/skill/CLI verbs unify; keep `run`/`propose` as deprecated aliases; bump the result-contract `schema_version`; update help/schemas/skill/AGENTS.md/MCP/README/site/docs/tests in lockstep; E26 router verbs now equal CLI verbs 1:1 (supersedes ADR-0031's verb-map) | Accepted |
| [0033](0033-cheaper-via-in-family-claude-tiering.md) | The default "cheaper" story is IN-FAMILY Claude tiering (frontier model authors predicates once -> kazi drives the grind on a cheap Claude model -> predicates keep it honest), token economy with NO local model or local GPU host; local/BYOM demoted to the privacy add-on; enable `claude --model` passthrough; benchmark leads with the Claude-tiering arm (refines ADR-0023/0030) | Accepted |
| [0034](0034-oss-contribution-gates-docs-with-code-no-leak.md) | OSS contribution gates: (1) docs land with the code in the same change (a surface change is unfinished without its docs); (2) no internal-info leakage (IPs/hosts/codenames/personal paths) in a public repo. Enforced at three layers -- CLAUDE.md (local+global), the `/apply` wave gate, and CI guards + a one-time scrub (E29) | Accepted |
| [0035](0035-skill-driven-adaptive-model-tiering.md) | Adaptive in-family model tiering is a SKILL recipe, never kazi-core: the skill defaults to frontier-authors -> cheap-Claude-grinds (ADR-0033) and ESCALATES the model (Haiku->Sonnet->Opus, capped) when kazi reports the loop stuck; kazi only exposes `--model` + reports state, the orchestrator owns the policy; auto-tiering inside kazi is rejected (refines ADR-0023/0033) | Accepted -- decision 1's LADDER LOCATION (skill-owned prose) superseded by ADR-0056 (`[escalation]` goal-file data); the no-policy-in-core principle RETAINED |
| [0036](0036-self-maintaining-docs-via-kazi-standing-goal.md) | Self-maintaining docs as a kazi standing goal: (L1) deterministic lossless trim of done+released epics to an append-only archive; (L2) gated knowledge extraction to the tier docs; (L3) doc-freshness predicates enforced in CI. kazi DRIVES it; the logic stays in the skill+CI layer (no doc engine in core). Fixes the kazi tier map (concept.md, not design.md). The flagship dogfood (refines ADR-0023; builds on 0033/0035) | Accepted |
| [0040](0040-generic-predicate-protocol-custom-script.md) | A generic `custom_script` predicate protocol -- the extensibility keystone: run any CLI checker with an EXPLICITLY-declared verdict (exit_zero/exit_code/json over stdout) + a SARIF/JUnit/JSON evidence parse, so a new kind of check ships as config not a kazi release; the SINGLE command-runner (test_runner/prod_log fold in as deprecated presets, removed in v2.0.0); defuses the exit-code gotchas (govulncheck/trivy/semgrep exit 0 with findings) and keeps `:error` distinct from `:fail` (refines ADR-0002) | Accepted |
| [0041](0041-predicate-envelope-v2-score-evidence-ratchet.md) | Predicate envelope v2: every result carries `{pass, score, prior_score, direction, evidence[]}` with LSP/SARIF/JUnit-shaped evidence, so the stuck-detector gets a gradient and fixers get localized fix context; adds a first-class `ratchet` mode (signal vs baseline) that coverage/perf/size collapse onto; additive non-breaking schema bump; the `:converged` gate stays the whole-vector `:pass` (refines ADR-0002) | Accepted |
| [0042](0042-anti-gaming-enforcement.md) | Anti-gaming ENFORCEMENT (makes "truth lives in the controller" real, not declarative): run the checker at the clean-tree + separate-process isolation level, read-only-lease predicate/test files, map skipped/errored/xfail to `:fail`, enforce test-count + coverage ratchets, a diff-inspection guard, and an optional held-out acceptance subset; default-on for creation mode; grounded in METR's 43x "sees the scoring function" result (refines ADR-0002) | Accepted |
| [0043](0043-predicate-catalog-expansion.md) | Predicate catalog expansion -- which checkers ship first-class vs `custom_script` config, and in what order: first-class `:static` (Dialyzer-led + polyglot SARIF), `:coverage` (ratchet), `:property` (PropCheck), `:mutation`, `:cve` (govulncheck reachability), plus live upgrades (sustained-health -> `:metrics` PromQL/RED -> SLO burn-rate -> synthetic journey); contract/perf/security-tail stay documented recipes; two evidence tiers (reachability vs presence) (depends on ADR-0040/0041/0042) | Accepted |
| [0044](0044-kazi-mcp-installed-subcommand.md) | `kazi mcp` as a first-class installed subcommand: promote the existing `Kazi.MCP.Server` (today reachable only via `mix kazi.mcp`) to a verb on the installed binary so an agent that `brew install`ed kazi can add it as an MCP server with `{command:"kazi",args:["mcp"]}` and learn the surface from tool schemas, not prose; `mix kazi.mcp` stays as the dev entry point (commits the surface ADR-0024 named) | Accepted |
| [0045](0045-context-store-layer-gist-provider.md) | A `context_store` layer distinct from `retrieval` (structural) and embedding recall: budget-fitted retrieval over heavy TEXT artifacts + repeated loop evidence, with `sirerun/gist` as the first provider (CLI adapter, in-memory/Postgres); the real payoff is evidence compression between iterations + a compact stuck-bundle for ADR-0035 escalation; redact-before-index; outer-indexes/inner-searches; opt-in `--context-store gist --context-budget N` with additive JSON stats (relates to ADR-0010/0012) | Accepted |
| [0046](0046-economy-accounting-envelope.md) | Economy accounting envelope: per-iteration usage split into input/cached_input/cache_write/output/reasoning/cost_usd + context/tools counters, so cached reads aren't budgeted as fresh input and the caching/stuck-bundle wins become falsifiable; KPI shifts to cost-per-converged-predicate; additive schema bump, `budget_spent.tokens` kept; honest-unknown fidelity marker (refines ADR-0023/0041) | Accepted |
| [0047](0047-inner-harness-minimalism.md) | Inner-harness minimalism: commit per-run tool-surface restriction now (`--strict-mcp-config`/`--tools`/exclude-flags -> fewer tool schemas in context, narrower action space, low-risk); define context-budget tiers 0-4 (default tier 1, escalate on non-progress) but gate the ladder thresholds on the E19 benchmark rather than hardcoding a guess; KPI is cost-per-converged-predicate (refines ADR-0008/0009/0010, needs ADR-0046) | Accepted |
| [0048](0048-adoption-blog-series-editorial-stance.md) | Adoption blog series -- editorial stance: credibility over hype, only shipped behavior, each of the twelve parts independently useful, walking a reader up the author's own ladder to a reconciliation workflow with kazi as the natural conclusion | Accepted |
| [0049](0049-approve-to-apply-handoff.md) | Close the `approve` -> `apply` handoff for orchestrator-driven runs: `apply` accepts an approved proposal ref directly (no goal-file round-trip required), `approve --write` can still materialize a goal-file for file-based workflows, `--json` stdout is a single clean object end to end | Accepted |
| [0050](0050-behavior-specs-gherkin-tier.md) | Behavior specs -- a Gherkin doc tier between ADRs and `goal.toml`, wired to the existing importer: `docs/specs/*.feature` behavior specs (Given/When/Then), wiring the already-shipped, previously-unused `Kazi.Reconcile.GherkinImporter` (ADR-0021/T13.2) into a CLI verb so predicates are DERIVED from a reviewed spec rather than hand-typed; extends the ADR-0036 tier map with a sixth tier | Accepted |
| [0051](0051-crystallize-discovered-truth-usecase-importer-prodlog-correlation.md) | Crystallize discovered truth -- an OPEN use-case-manifest schema + a harness-driven use-case-discovery step + prod-log predicate correlation | Accepted -- decisions 1-3 (schema/discover/importer) SUPERSEDED by ADR-0054; decision 4 (prod-log correlation) RETAINED |
| [0052](0052-self-teaching-docs-no-personal-skill-assumption.md) | Self-teaching artifacts must not assume the operator's personal skill library: `AGENTS.md` + the `kazi install-skill`-generated `SKILL.md` named `/plan`/`/tidy`/`/loop`/`/qualify` as if universal (root-caused to ADR-0024/0026/0031's operator-centric framing, the same "audience of one" mistake ADR-0015 already rejected once); rewrite both to describe the contract generically, add a coherence guard against recurrence, and retire the never-functional `Kazi.Retrieval.Graphify` backend (shelled out to a `graphify` CLI that does not exist -- `/graphify` is an agent-executed skill, not a binary) while keeping the pluggable retrieval seam intact | Accepted |
| [0053](0053-higher-level-interactive-surface-predicates.md) | Higher-level interactive-surface predicates -- a `:browser` UI-assertion pack (`console_clean`/`a11y`/`visual`/`form_validation`/richer-DOM/viewport, runner + schema only) plus a first-class `:cli` provider that drives the SHIPPED binary as a user invokes it (exit/stdout/stderr matrix + `--help` golden), dogfooded against kazi's own release binary to catch the release-boundary bugs `:tests` is blind to; mobile/TUI/API-contract/Lighthouse stay documented recipes until a goal earns promotion (extends ADR-0043) | Accepted |
| [0054](0054-product-usecases-are-gherkin-not-bespoke-manifest.md) | Product-level use cases are Gherkin, not a bespoke manifest schema -- supersedes ADR-0051 decisions 1-3: withdraws the invented `usecase-manifest.json` schema (self-inconsistent with ADR-0050's own Gherkin choice, and had zero producers anywhere); a product's use cases are tagged Gherkin Scenarios (`@role:`/`@priority:`/`@interface:`, real Cucumber tags) imported via the SAME extended `GherkinImporter`/`kazi spec import` path; discovery for an existing codebase is `kazi init --discover` writing an iteratively-CONVERGED manifest-coverage goal (reusing the shipped surface-scanner, T13.4) instead of a one-shot exhaustive-audit prompt; retains ADR-0051 decision 4 (prod-log correlation) unchanged | Accepted |
| [0055](0055-landing-is-part-of-convergence.md) | Landing is part of convergence: an `[integration]` goal-file block (commit/branch/pr/merge/none) enforced as an implicit `landed` predicate (never a decide/2 special case), Integrate stops bulk-committing for the agent (verify clean committed branch -> push -> PR -> rebase-merge), a versioned controller-owned PROCESS CONTRACT in the dispatch prompt instead of prose discipline blocks in goal-files, per-group branch/PR landing in `needs` order under `--parallel` with `git cherry` silent-revert verification, first-class `no_stubs`/`oss_hygiene`/`docs_updated` gate providers, and an apply preflight (closes the gap ADR-0031 left; refines ADR-0026/0027/0042) | Accepted |
| [0056](0056-one-system-kazi-subsumes-plan-apply-skills.md) | One system: kazi subsumes the operator's plan/apply orchestration skills for ENGINEERING work -- `kazi plan --project` authors a roadmap as a goal DAG (`needs` edges between goals, ADR-0028 lifted one level); rolling-wave is native (an outline phase is a goal whose predicate is "phase N's goal-set exists, floor-passed, approved" -- planning itself converges); `kazi plan render` makes the plan document a generated VIEW of the read-model (never input); `kazi plan --discover` unifies the authoring on-ramp; the escalation ladder becomes `[escalation]` goal-file DATA (supersedes ADR-0035 decision 1's skill-owned-ladder location; core still holds no selection policy); non-engineering explicitly OUT; skill retirement gated on a zero-skill idea->landed-PR->live-verify dogfood (extends ADR-0031; ADR-0026 becomes historical on proof) | Accepted |
| [0057](0057-fleet-observability-dashboard.md) | Fleet observability: the run registry, per-run sinks, and `kazi dashboard` (home-view decision superseded by 0070) | Accepted |
| [0058](0058-economy-feedback-loop.md) | Economy feedback loop: persisted run economics, learned budgets, behavior-first prompt improvement, and honest budget stops | Accepted |
| [0059](0059-goal-file-lifecycle-and-organization.md) | Goal-file lifecycle: a conventional directory, sequence-numbered chronology, and archive-on-converge | Accepted |
| [0060](0060-memory-is-kazi-core-four-layers.md) | Memory is kazi-core: four layers of controller state (working = evidence/orientation 0009/0010, episodic = attempt ledger 0061, semantic = git-native recall 0062, statistical = economy 0046/0058) under one owner -- memory is the controller's integral term, not an adjacent feature; three guardrails (librarian-never-vault, gated writes, every layer pays measured rent via the 0046 envelope); one surface (`kazi memory` + `kazi mcp`); sibling memory tool rejected (unclosable feedback loop + silent-seam failure class); refines 0008's statelessness (controller carries state, inner calls stay stateless) | Accepted |
| [0061](0061-episodic-memory-attempt-ledger.md) | Episodic memory: a deterministic attempt ledger -- a fold over recorded iteration history (verdict transitions, touched files, error fingerprints; never model prose) injected as a bounded, tier-gated dispatch-prompt section so the inner model stops repeating known-failed approaches; fingerprint-based repeat detection; `decide/2` and the prompt read the SAME fold; cross-run for the same goal; flag-gated until the 0046 benchmark proves rent (refines 0008/0009/0047/0058) | Accepted |
| [0062](0062-semantic-memory-git-native-recall.md) | Semantic memory: store of record is repo markdown (lore/ADRs/devlog/AGENTS.md, `[memory]`-configurable), indexed via SQLite FTS5 in the read-model (zero new deps; embeddings require a benchmark + superseding ADR); the API is budgeted recall (ranked slice guaranteed to fit a token budget, `path:line` sources) surfaced to the loop, `kazi memory recall`, and `kazi mcp`; recall is read-only and corpus files are `read_only_paths` during runs; 0012's retrieval seam gets its first functional default, 0045's external-provider ambition retires (Gist legacy) | Accepted |
| [0063](0063-gated-memory-writes-harvest-promotion.md) | Memory writes are gated: debrief-time harvest emits detector-originated candidates (models may phrase, never originate) to a `proposed_memories` table; promotion is `kazi memory list-proposed/approve/reject` writing human-reviewed diffs to repo docs routed by the 0036 tier map; mandatory provenance (`kx:` trailers, run/goal/evidence refs); the inner agent has no write path (0042 `read_only_paths`); explicit decay with optionally predicate-verifiable claims and a staleness ratchet -- closes the facts -> beliefs -> recall -> measured-effect lifecycle | Accepted |
| [0064](0064-scenario-predicates-demonstrate-then-pin.md) | Scenario predicates -- capability-level verification by demonstrate-then-pin: a first-class `scenario` provider binds a tagged Gherkin Scenario ("a user can create and download a PAT") to a committed PIN -- a replayable trace in the existing `:browser`/`:cli` config vocabulary (E43 as compile target) -- and passes ONLY when the pin validates (every When->step, every Then->assertion, Scenario-hash current) and replays green; pins are authored/repaired by a DEMONSTRATOR dispatch role write-disjoint from the fixer (role-scoped 0042 `read_only_paths`: demonstrator writes only pins, fixer never does), so agentic nondeterminism is quarantined at authoring time and evaluation stays deterministic; repin-with-diff distinguishes selector rot from capability regression; extends ADR-0054 d3 lowering opt-in (`@interface:web|cli` -> `scenario`); standing goals get capability monitors (the DoD "verified live" step as a predicate) | Accepted |
| [0065](0065-safe-concurrent-work-serial-worktree-fleet.md) | Safe concurrent work: serial apply becomes the 1-partition degenerate case of the E21 scheduler (kazi owns the task worktree; `--workspace` = the base to integrate onto; `--in-place` opts out), integration per T21.5 (never a tree-wide reset/clean), and `--fleet` runs a DAG of goal-files reusing E21 worktrees/leases/blast-radius/budget machinery one level up; the #936 supervised-checkpoint mode lands as one mechanism for wave and fleet-node boundaries; single-node, NATS-free (responds to #937 Gaps A/D) | Proposed |
| [0066](0066-burrito-fork-payload-liveness-guard.md) | Fork burrito for a payload liveness guard: builds pin the kazi-org/burrito fork whose maintenance pass skips payload dirs registered live via `.burrito_live/<pid>` pidfiles (EPERM counts as alive; stale pidfiles pruned) and honors `BURRITO_NO_CLEAN_OLD`, closing the mid-run `io_lib_pretty nofile` kernel-crash class (#1018); proposed upstream, fork reverts to hex when it lands | Accepted |
| [0067](0067-session-coordination-bus.md) | Session coordination bus: operator sessions become first-class participants on the ADR-0004 JetStream substrate via a new `kazi daemon` (supervised local nats-server) -- presence as TTL'd KV, facts as last-value subjects, intents/events as an age-bounded stream, directed messages via per-session durable consumers (the cursor); token economy enforced server-side (digests, size caps, severity gating) so context cost is decoupled from message volume; surfaces are `kazi bus` CLI + `kazi mcp` tools with hook-based delivery documented, messages are advisory and provenance-stamped; convergence never depends on the bus; activates Slice 3's NATS dependency; non-goals: agent-org semantics (0048), task queueing/locking (0002/0006), memory (0060-0063) | Accepted |
| [0068](0068-daemon-single-writer-read-model.md) | The kazi daemon is the single writer for the read-model: with the daemon running, CLI/run processes write via its versioned local API (Unix socket) instead of opening `kazi.db` directly -- one writer, one migration set, eliminating the #1019 mixed-version deadlock/blind-run class structurally; only the daemon migrates; version skew becomes an explicit handshake ("restart daemon or continue without persistence"), never a lock fight; reads may stay direct (WAL); no daemon = today's behavior unchanged; the daemon also hosts ADR-0005's ETS live layer for the dashboard | Accepted |
| [0069](0069-prompt-cache-economics-observability-and-argv-pinning.md) | Prompt-cache economics: `kazi economy` groups gain a `cached_input_tokens` percentile pair + `cache_read_ratio` (from the already-persisted T34.2/T48.7 split, honest-unknown); the dormant `--exclude-dynamic-system-prompt-sections` lever and a new `--bare` economy flag become operator-reachable via CLI/goal-file passthrough (OFF by default, byte-identical argv pinned); default flips ship ONLY via the T48.12 benchmark gate, with the bare-mode fixture set required to include a CLAUDE.md-dependent goal; statelessness (0008/0009) explicitly not relitigated (extends 0047/0058) | Proposed |
| [0070](0070-mission-control-dashboard.md) | Mission Control: the fleet home view becomes an ops-center card grid (`KaziWeb.MissionControlLive`), superseding ADR-0057's starmap constellation home view only (registry/sinks/attention/drill-in unchanged); per-card predicate DNA + iteration burn + convergence sparkline inline from the read-model; honest iteration-budget bar (no token cap invented, ADR-0046); the ADR-0056 roadmap DAG preserved as topological wave-grouped card sections (`--roadmap`, T47.2 kept alive); read-only projection reaffirmed (ADR-0011 §2), starmap filters/slide-over/mobile-tab-bar retired for plain grid reflow | Accepted |
| [0076](0076-bus-delivery-is-installed-not-documented.md) | Bus delivery is installed, not documented: supersedes ADR-0067 point 6's "the hook recipe ships as documentation" (observed install rate: zero, with a daemon live for 16h across two machines) with an opt-in `kazi install-hooks` installer -- session-start registers presence/team and injects the board, turn-boundary injects the digest only when there is traffic (zero token cost when quiet, no blocked `watch`, no poll loop); binding rule: only events whose stdout reaches the session's context (Claude Code: SessionStart + UserPromptSubmit -- a Stop hook's output never reaches the next turn); the installed command is a `kazi bus hook <event>` subcommand (in-binary, testable, version-skew-safe) with a hard wall-clock bound (silent exit on expiry -- a hung daemon must never tax every turn); default target is USER-level settings, `--project` writes the local uncommitted file, never a committed file in a public repo (0034); merge-never-clobber + idempotent + `--uninstall`; no-ops without a daemon (0067 point 1), injected content stays advisory (0067 point 7); ADR-0001/0008 govern the harness kazi DISPATCHES, not the operator's own driving session (0067 point 2), and `install-skill` (0024 decision 1) already sets the opt-in precedent; non-goals: runtime coupling, auto-install, chat UI | Accepted |
| [0072](0072-bus-digest-protects-the-machine-path.md) | The bus digest protects the machine path: supersedes ADR-0067 point 5's ~1 KB producer cap as the enforcement mechanism while reaffirming its goal ("token cost decoupled from message volume by design, not client discipline") -- the shipped digest is a CLI-only pure function reached solely from the TTY branch of `emit/3`, so `--json`/MCP (every agent path) get the full transcript; the digest becomes the default on every machine-readable surface (`--full` escapes), stored size is decoupled from context cost (64 KiB/128 KiB/30d limits KEPT -- oversized bodies render as a one-line stub + id, pulled deliberately via `bus get <id>`), discipline moves from producer to render, and assembly moves server-side into the daemon with a tested line bound; ids are the JetStream stream sequence (today stripped before return) so every digest line is dereferenceable, and the digest JSON joins the ADR-0023 versioned contract (`schema_version`, `kazi schema`) that today's bare `{ok, messages}` envelope never honoured; realizes E51's R-E51-4, whose three mitigations were all raised, deferred, or blocked | Accepted |
| [0073](0073-the-board-current-state-claims-identity.md) | Teams need a board: extends ADR-0067 by readmitting the *interaction model* of its rejected filesystem-blackboard alternative (the implementation rejection stands) -- two teams independently rebuilt append-only markdown blackboards and never returned, losing cross-machine because the files are machine-local; a stream answers "what changed", a team needs "what is true now", and `fact`'s last-value retention (point 3) was already a board with no verb to render it; adds `kazi bus board` (current facts + roster + claims, cursor-free, bounded by 0072), claim visibility by reading `refs/claims/*` AT SOURCE (the refs are already cross-machine and self-describing -- no copy to drift, no daemon in the claim path, honest one-line degradation offline; auto-posted claim facts rejected as a second copy of truth AND as unshippable, since the claim tooling lives outside kazi's delivery -- 0067 point 6's failure in a new coat; an optional best-effort claim-event post feeds digests only), stable addressable identity (`bus name`, killing the `os-<pid>` ghost rows), and a dashboard that renders the board instead of "No instances present"; non-goals: lock service, task queue, chat UI, org chart (0048) | Accepted |
| [0079](0079-velocity-data-architecture.md) | Velocity data architecture (E67 full-fusion): the "agent delivery" identity is spined on the immutable E65 session UUID, joined through the RUN REGISTRY (`run.harness_session_id`/`goal_ref`/economics), NOT the `Claude-Session:` commit trailer -- undocumented in-repo and actively stripped by this repo's hygiene, so a trailer-dependent design would attribute nothing on kazi itself (#1483 empty-table class); trailer kept only as a nullable, optional enrichment for repos that retain it, unjoinable delivery is honest fleet-level with a `nil` session_uuid; a per-machine OPT-IN session-stats collector reads local transcript JSONL and emits ONLY a closed whitelist of aggregate counters (cached/fresh/output/reasoning tokens, message/tool-call counts, active-time buckets -- NEVER content, tool names, or paths), shipped as `session:<short-uuid>` bus FACTS (last-value-per-subject, idempotent by construction) through the daemon single-writer path (0067/0068), incremental via a machine-local transcript cursor; two new read-projection tables (`delivery_events`, `session_counters`) with NO ETA/estimate column; display rules are rate/ratio/distribution only reusing T63.9's honest-`nil` vocabulary (delivered/day, tokens-per-delivered-task, stuck ratio, claim->merge lead-time as a p50/p90 DISTRIBUTION reconciled with `kazi economy`, never a promised date -- 0046); constraints named: 0011 projection, 0046 honest-unknown, 0068 daemon-writer, 0067 bus | Accepted |
| [0080](0080-sealed-predicates-tamper-detection.md) | Sealed predicates -- cryptographic tamper detection on the acceptance contract: at run start content-hash the goal-file (always, implicitly) plus every path a `[seal] sealed_inputs` glob names (manifests, reference images, fixtures), re-verify every observe pass, and make a mismatch a FATAL new terminal `tampered` status naming the offending file -- never advisory, never collapsible to `converged` (restoring the objective-termination guarantee ADR-0001 promises for the goals that opt in); `mutable_inputs` is a subtractive per-file opt-out and `enabled = false` the whole-run opt-out for self-modifying goals; orthogonal to and strictly stronger than the advisory ADR-0042 `read_only_paths` flag | Accepted |
| [0081](0081-capture-recipes-run-evidence-store.md) | Capture recipes + run evidence store: UI goals are gamed by presence (a text/id grep cannot tell "renders" from "id pinned on a stock component"), and a worker-run screenshot is a CLAIM not evidence (a cached build lies); declare named `[[capture]]` recipes (`reset_cmd`/`launch_cmd`/`output`, injectable `CommandRunner` seam) the CONTROLLER executes each observe pass at the ADR-0080 seal-verify precedence, writing each artifact + a provenance sidecar (cmd lines, sha256, size) into a run-keyed evidence store under `<sinks_dir>/<run_id>/captures/` OUTSIDE the workspace (that separation IS the worker write-protection); predicates consume a capture BY NAME (`input = "capture:<name>"` -> `context[:captures]`), and a built-in `render_proof` predicate fails a blank/crash/degenerate frame so code that compiles but never renders cannot converge; captures surface per-iteration in `status --json`; the `[[capture]]` block is implicitly sealed (0080) -- input-side twin of 0080's contract-side immutability; live browser/simulator capture is the hermetic `CommandRunner` seam only, stubbed in tests | Accepted |
